# Green Área — Jornada Pokémon (protótipo)

- **Código-fonte**: [`index.html`](index.html) — página HTML única, sem build, sem servidor (mesmo espírito do [Diário de Treinador](../pokemon-rpg/README.md)).
- **Site publicado**: `https://<seu-usuário>.github.io/<repositório>/green-area/` (mesma configuração de GitHub Pages já ativada pro resto do repositório — nada novo pra configurar).

Pedido do usuário: um site **independente** do Diário de Treinador — um jogo de completar a Pokédex. O Treinador recebe um Pokémon **inicial aleatório** (de qualquer Geração 1–9), viaja capturando Pokémon selvagens em lutas **automáticas**, enfrenta 8 **Mestres Especialistas** por Insígnias, e tudo isso limitado por uma barra de **Energia** que recarrega com o tempo.

O tema visual pedido pro primeiro teste: **Green Área**, um "ambiente de testes" fictício de uma empresa de jogos — dá o tom pro visual (verde/preto, "build de testes" no canto, um painel `🛠️ Ferramentas de Teste` honesto sobre o que é atalho de debug e o que é jogo de verdade).

## "Usando as regras daqui" — o que foi reaproveitado do RPG

Nada foi digitado de novo: `SPECIES_DEX` (1025 espécies, Stats Base reais inclusos), `TYPE_CHART` (a tabela multiplicativa oficial), `MOVES_DEX` (847 Movimentos), `LEARNSETS` e `EVO_STAGE` são **extraídos de verdade** do próprio [`docs/pokemon-rpg/site/diario-de-treinador.html`](../pokemon-rpg/site/diario-de-treinador.html) por um script Node (não é uma cópia colada à mão, então nunca diverge dos dados oficiais do RPG por erro de digitação) — ver `EVO_PREV`/`EVO_STAGE` etc. já documentados no [README do RPG](../pokemon-rpg/README.md).

**Motor de combate fiel ao jogo oficial (Bulbapedia/PokeAPI)** (Cap. "vamos usar o sistema de combate baseado no original, pegando informações do bulbapedia... quero um sistema de combate bem fiel"): diferente do RPG principal (que usa uma adaptação de mesa com d10 e Tipo somando/subtraindo), aqui o motor segue a fórmula OFICIAL de verdade:

```
Atributo (PV) = piso((2×Base + IV + piso(EV/4)) × Nível/100) + Nível + 10
Atributo (outro) = piso((piso((2×Base + IV + piso(EV/4)) × Nível/100) + 5) × Natureza)
Dano = (piso(((2×Nível/5+2) × Poder × Ataque/Defesa)/50) + 2) × STAB(1,5) × Tipo × Crítico(1,5) × aleatório(0,85–1,00) × (0,5 se Queimado e Físico)
```

— IV fixo em **31** (perfeito) e EV fixo em **0** pra todo Pokémon (documentado: sem mini-jogo de treino de Atributo nesta leva, fora do escopo — fidelidade de COMBATE, não a metagame de treino inteira); Natureza usa o multiplicador real **×1,1/×0,9** (não mais ±1 fixo); Tipo usa a tabela **multiplicativa** real (`TYPE_CHART`, ×0/×0,25/×0,5/×1/×2/×4 — já era o dado oficial, só passou a ser CONSUMIDO multiplicando); Crítico é **1/24 base** (dobra por Estágio, via `critRate` de cada Movimento), não mais um dado; Acerto é **porcentagem real** (não mais limiar de d10), ajustado por Estágio de Precisão/Evasão do jogo oficial (-6 a +6, tabela de fração oficial).

O **efeito de execução de cada Movimento também é o oficial de verdade** — `MOVES_OFICIAL`, dados estruturados buscados via PokeAPI (poder, precisão, PP, prioridade, taxa de crítico, dreno, cura, chance de Hesitação, Condição/chance, mudança de Estágio, alvo, categoria) pra todos os 847 Movimentos, aplicados por um motor GENÉRICO orientado a dados (mesma filosofia do resto do projeto: regra sobre dado, não tabela gigante escrita à mão) — cobre a esmagadora maioria dos Movimentos automaticamente, com PP real consumido de verdade (`MOVES_OFICIAL[*].pp`) e **Struggle** como último recurso quando acaba o PP de tudo (recuo de 1/4 do próprio PV Máximo). O **Papel de Combate** (`⚔️`/`🛡️`/`🌿`/`🌀`) continua existindo, mas agora é só a camada de **seleção** de qual Movimento usar — a execução nunca mais depende do papel, é sempre o efeito oficial real do Movimento escolhido.

A única peça nova de verdade: o RPG nunca teve "Nível de Pokémon" separado do Nível do Treinador. Aqui, cada Pokémon tem o próprio Nível — **1 a 100** (um teto de 16 copiado do Treinador do RPG de mesa chegou a existir por 1 leva, mas o pedido seguinte foi "o nível pode voltar pro 1 ao 100... vamos manter tudo igual ao do original mesmo").

**Sistema de level up — por XP e Faixa de Nível** (substitui o antigo "4 vitórias = +1 Nível"): cada Pokémon tem sua própria "Faixa" (bloco de 10 Níveis — 1–10, 11–20... até 91–100, a MESMA divisão das 10 Zonas de captura, ver abaixo). Cap. leva desta sessão ("Pokémon dentro da sua range de nível ganhará 2xp por combate vencido, 1 categoria abaixo ganhará 1 e a cada categoria acima irá ser somado 1 de xp aos 2 de base. Categorias abaixo de 1 da base não dão XP") — vencer um oponente na PRÓPRIA Faixa dá **2 XP** (base); **1 Faixa ABAIXO** dá **1 XP**; **2+ Faixas ABAIXO** dá **0 XP** (senão dava pra "evoluir infinito" farmando selvagem fraco); cada Faixa **ACIMA** soma **+1 XP** aos 2 de base (1 Faixa acima = 3 XP, 2 = 4 XP, 3 = 5 XP...). **10 XP fecha 1 Nível** (5 vitórias na própria Faixa, já que cada uma vale 2 XP agora). Contra Treinador (aleatório de Zona OU Mestre Especialista), o XP é creditado **por CADA Pokémon do time dele que cai** — "se ele tiver 6 Pokémon e meu Pokémon vencer os 6, ele recebe 6 pontos" — e isso acontece **NO MEIO DA LUTA**, oponente a oponente, não só no fim: dá pra subir de Nível (e até evoluir, ver abaixo) sem a luta ter acabado ainda. O battler ativo é sincronizado na hora (o resto da luta já usa o Nível/PV Máximo novo).

**Evolução de verdade, com o Treinador decidindo**: quando um Nível sobe e a Espécie já bate o requisito real de evolução (`EVOLUTIONS_DEX`, extraído do próprio repositório da PokeAPI — quem evolui pra quem e em que Nível, nunca inventado), a luta **pausa** e pergunta "Evoluir?". Aceitar troca a Espécie preservando os Movimentos já conhecidos (evoluir nunca reseta o kit — mesma "identidade" que a Troca de Movimento protege) e a Natureza (nunca muda), remapeando a Habilidade pelo mesmo "slot" na lista `hab` da nova Espécie quando existe. Recusar mantém a forma atual — e **"se ele impedir, todo Nível, pergunte de novo"**: não existe flag de "recusado pra sempre", a próxima vez que o Nível bater o requisito a pergunta volta. Evoluções que no jogo oficial dependem de pedra/troca/felicidade (este protótipo não tem esses itens/mecânicas) usam como limiar aproximado o `nivelTipico` da forma evoluída (ver Zonas, abaixo) — documentado, mesmo espírito de aproximação do resto do arquivo.

**Natureza e Habilidade — dados 100% reais do RPG, sem inventar nada** (Cap. "não temos necessidade de criar muita coisa nova... é só copiar do site"): `NATURES_DEX` (as 25 Naturezas oficiais, agora aplicadas com o multiplicador real ×1,1/×0,9) e `ABILITIES_DEX` + o campo `hab` de cada `SPECIES_DEX` (a lista REAL e LEGAL de Habilidades daquela Espécie, incluindo a Habilidade Oculta) são as MESMAS estruturas já usadas no Diário de Treinador — nenhuma copiada/reinventada. Sorteadas 1x quando o Pokémon nasce/é capturado ("capturou ou ganhou... vem com a hability e nature random dentro da limitação dele") e **nunca mudam depois** (evoluir remapeia a Habilidade pro slot equivalente, mas nunca sorteia de novo do zero salvo quando o slot não existe na forma nova) — não existe (ainda) um item pra re-rolar por vontade própria.

**"Sempre que a luta acaba, o Pokémon é totalmente recuperado"** (pedido explícito, mantido desde sempre e agora também vale pra PP/Estágio/Condição, não só PV): PV, PP, Estágio (-6..+6) e Condição/Confusão vivem só no battler EFÊMERO da luta (nunca no registro permanente da Equipe) — um battler novo é montado toda vez que o Pokémon entra numa luta nova, então sai automaticamente "de Centro Pokémon" sem precisar resetar nada à mão.

## Habilitar o salvamento online (1 passo manual, único)

O código já está pronto e usa o Firebase do Diário de Treinador — só falta autorizar as coleções novas nas regras de segurança do Firestore, porque as regras atuais só liberam o documento `diario/estado` do RPG e bloqueiam tudo o mais (`match /{document=**} { allow read, write: if false; }`). Cap. leva do Centro Pokémon (Fases 1 a 3): 6 coleções A MAIS, além de `green_area_saves` -- `green_area_chat_publico` (chat público), `green_area_inbox` (pedidos de amizade recebidos), `green_area_amizades` (amizades confirmadas), `green_area_centro_presenca` (quem está com a tela do Centro Pokémon aberta AGORA), `green_area_dm` (chat privado entre amigos) e `green_area_trocas` (troca ao vivo de Pokémon/itens/dinheiro) -- ver "Centro Pokémon", "💬 Mensagens" e "🔄 Troca ao vivo" mais abaixo pro que cada uma guarda.

**Se você já configurou as regras antes e agora está vendo "Não consegui enviar o pedido"/"Não consegui mandar a mensagem" (pública OU privada)/erro ao trocar no Centro Pokémon**: é quase certo que é exatamente isso -- as regras antigas não autorizam as coleções novas das levas mais recentes. Repita os passos abaixo com o bloco atualizado; a mensagem de erro agora mostra o motivo real do Firestore (não mais um genérico "verifique sua internet"), então dá pra confirmar comparando com o texto que aparecer na tela.

1. Acesse [console.firebase.google.com](https://console.firebase.google.com/), abra o projeto **diario-de-treinador** (o mesmo do RPG).
2. Vá em **Compilação → Firestore Database → Regras**.
3. Substitua o conteúdo pelo bloco abaixo (adiciona a liberação de `green_area_saves` + as 6 coleções do Centro Pokémon, mantendo a do RPG intacta):
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /diario/estado {
         allow read, write: if true;
       }
       match /green_area_saves/{saveId} {
         allow read, write: if true;
       }
       match /green_area_chat_publico/{msgId} {
         allow read, write: if true;
       }
       match /green_area_inbox/{saveId} {
         allow read, write: if true;
       }
       match /green_area_amizades/{parId} {
         allow read, write: if true;
       }
       match /green_area_centro_presenca/{saveId} {
         allow read, write: if true;
       }
       match /green_area_dm/{conversaId} {
         allow read, write: if true;
       }
       match /green_area_trocas/{tradeId} {
         allow read, write: if true;
       }
       match /{document=**} {
         allow read, write: if false;
       }
     }
   }
   ```
4. Clique em **Publicar**.

Pronto — depois disso o login funciona (Entrar/Criar novo na tela inicial). Sem esse passo, criar conta ou entrar mostra um erro de conexão com o banco (a regra atual recusaria a escrita/leitura na coleção nova).

## O que o protótipo já faz

- **Login/criação de conta (Nome+PIN)**: entra com Nome+PIN de um Treinador já criado, ou cria um novo — o inicial é sorteado na hora da criação (1 das 9 Gerações, depois 1 dos 3 daquela Geração — pra dar a mesma chance a cada Geração, não a cada Pokémon). O progresso fica salvo no Firebase (ver "Salvamento" abaixo), então dá pra entrar de novo em qualquer aparelho com o mesmo Nome+PIN.
- **Pokédex** (`📕`): grade com as 1025 espécies. Nunca visto = cinza escuro e sem contorno; visto em batalha = cinza claro (contorno neutro); **capturado = colorido** (mesma imagem, só o filtro CSS muda — a "foto" é a mesma fonte de sprite já usada e comprovada no Diário de Treinador, PokeAPI/sprites; Bulbapedia em si não tem um CDN estável pra hotlink direto, essa é a mesma substituição confiável que o resto do projeto já usa). Clique num quadro pra ver detalhes: quem **nunca foi visto** só ganha a dica de **📍 Onde encontrar**; quem já foi **visto** (mesmo sem capturar — a própria tela de batalha já mostra o nome) libera tudo: Tipo(s), **🗡️ Movimentos possíveis** (Learnset real), **✨ Habilidades possíveis** (`hab` real, marcando a Oculta), **📜 Lore em pt-BR (as 1025 Espécies, ver abaixo)** e o **🎵 Cry original** (áudio oficial, mesmo CDN público da PokeAPI) — Stats Base continuam exigindo captura.
- **Lore traduzida pra pt-BR — as 1025 Espécies, sem exceção** (Cap. "a lore dos pokémon tem que ser traduzida pra pt-br... vá atrás das informações no bulbapedia"): `LORE_PT_DEX` cobre a Pokédex inteira, traduzida à mão a partir do texto oficial em inglês (mesmo conteúdo que aparece no Bulbapedia) — a PokeAPI, verificado, não tem NENHUMA entrada em pt-BR pra puxar pronta, então não havia como buscar isso ao vivo. Sem fallback nenhum precisando entrar em ação: toda Espécie mostra a Lore em português assim que o popup da Pokédex abre.
- **Área de Captura** (`🌿`) — **10 Zonas nomeadas, uma por Faixa de 10 Níveis** (1–10 até 91–100: Aprendiz, Iniciante, Novato, Competente, Habilidoso, Avançado, Experiente, Veterano, Elite, Lendário). **Nenhuma Zona trava por Nível mínimo** ("libere o acesso para todas as zonas com qualquer pokémon, a responsabilidade de acesso é do treinador") — qualquer Pokémon pode liderar em qualquer Zona; a tela só troca o cadeado por um aviso ⚠️ quando o líder está abaixo do Nível recomendado. Quem aparece selvagem em cada Zona vem de um `nivelTipico` calculado com dois sinais reais (nunca inventados): Estágio de evolução (`EVO_STAGE`) + Total de Atributos Base (`BST`) — formas não-evoluídas cedo, evoluções finais tarde (resolve de propósito o risco citado no pedido: "não ocorra risco de termos um GOLEM no nível 1-10" — com esse cálculo, o Golem só aparece a partir da Zona Veterano/71+). Lendário/Mítico (`LEGENDARY_NUMS`, dado real da PokeAPI) nunca aparece selvagem em Zona nenhuma. A chance de cair num **Treinador aleatório** em vez de selvagem cresce com a Zona (15% na Aprendiz até 87% na Elite) — e vira **100% Treinador** se a piscina selvagem daquela Zona esgotar de vez. Só **depois de vencer** um selvagem é que oferece jogar Pokébola. Se a luta escolhida for **muito acima do que a Equipe inteira aguenta** (nem o Pokémon mais forte do time chega perto — mesma régua de Faixa do XP), um popup ⚠️ avisa e pede confirmação antes de gastar Energia. **Lutar Várias Vezes** (Cap. leva desta sessão: "o treinador pode digitar a quantidade de vezes que ele quer ir... ou dar a opção de ele usar TODA a energia disponível" + leva seguinte, automação total): cada Zona tem um campo pra digitar quantas lutas seguidas fazer (`🔁 Lutar Várias Vezes`) ou um botão que já calcula o máximo possível com a Energia atual (`⚡ Usar Toda a Energia`). **A quantidade pedida NUNCA passa da Energia ATUAL** ("se ele tem 100 de energia e vai fazer uma luta que custa 5, ele pode lutar no máximo 20") — mas, ao contrário da leva anterior, pedir mais do que cabe **não deixa nenhuma luta começar**: o banner do topo avisa o máximo real ANTES de qualquer coisa, e o campo volta sozinho pra esse número, pronto pro jogador clicar de novo já certo. Pedir **1 luta só** continua sendo o clique manual de sempre. Pedir **2 ou mais** abre um popup perguntando, com 2 toggles **independentes**, se o jogo deve **🎯 tentar capturar automaticamente** e/ou **💊 curar automaticamente** durante a sequência — se nenhum dos dois for marcado, a sequência roda do mesmo jeito, só que sem gastar item ou Pokébola nenhuma. A partir daí a sequência é **totalmente automática**: lutas se encadeiam sozinhas, gatilho de cura e Pokébola (quando ligados) usam sempre o "item/bola mais recomendado" pra situação (o menor item que já cobre o PV que falta; a bola mais fraca que já bate ~70% de chance de captura), troca por desmaio é sempre resolvida sozinha, e **evolução nunca é decidida no meio do caminho** — fica pendente pra perguntar só depois que TODA a sequência termina, um Pokémon de cada vez, numa tela de resumo com o placar final (vitórias/derrotas) e, junto, um **relatório detalhado** (Cap. leva desta sessão: "gere um relatório final de cada luta como foi cada uma e o resultado, se teve captura, uso de poção etc") -- 1 linha por luta (número, tipo de oponente, resultado, Pokédólares/Pokétickets ganhos, item de cura usado, tentativa de captura -- sucesso ou fuga -- e se precisou trocar de Pokémon), cada uma abrindo (`<details>` nativo, sem clique em nada além dela mesma) pra mostrar o log turno-a-turno completo daquela luta específica. Enquanto uma sequência está ativa, nenhuma luta manual nova pode começar por fora dela. **O estado inteiro vive salvo no Firestore** (não só na aba aberta) — atualizar a página, trocar de tela ou fechar e abrir de novo não perde nada: um relógio de fundo (mesmo padrão do contador de Energia) continua resolvendo uma luta por vez sozinho, e voltar na Área de Captura mostra exatamente onde a sequência está.
- **Mestres Especialistas** (`🎖️`): 8 Mestres temáticos por Tipo, agora com **6 Pokémon cada** (era 3, depois 5), cada um de uma **família evolutiva DIFERENTE** ("não fica repetindo pokémon em um mesmo mestre... pode ter um tipo que ele prefira, mas que os pokémon sejam diferentes") — verificado por script contra os dados reais de evolução da PokeAPI, nunca duas formas do mesmo Pokémon no time. Ace bem mais forte (`25 + índice×10`: 25, 35, 45... até 95 no 8º). Elenco **100% fixo** — o mesmo time pra QUALQUER Treinador que desafiar aquele Mestre. Ícone do Mestre = sprite pixelado real do próprio Ace (não existe uma "Insígnia oficial" pra Mestre inventado aqui, então o ícone usa o Pokémon de verdade). Botão **👁️ Ver Time** em TODA linha da lista mostra o elenco completo, Nível incluso, sem precisar desafiar. Progressão **sempre sequencial**. Vencer dá a Insígnia — e, se sobrar alguma Espécie do time vencido com Movimento de MT, também 1 **MT de graça**.
- **Loja** (`🏪`): vende **4 tipos de Pokébola** — Pokébola, Great Ball, Super Ball e Ultra Ball, cada uma com **descrição de captura própria** (multiplicador ×1,00/×1,50/×1,75/×2,00 sobre a Taxa de Captura oficial da Espécie) — e as 3 Poções, cada uma com a descrição exata de quanto PV restaura. Ícones pixelados oficiais (mesmo CDN de sempre, pasta `items/` da PokeAPI/sprites) em vez de emoji genérico. Compra tudo com **Pokédólares**, ganhos **depois de toda luta vencida**, somados **por CADA oponente derrotado**, com multiplicador que sobe conforme o tipo de luta — selvagem (×5) < Treinador aleatório (×12) < Mestre Especialista (×30). Também vende **MTs** (💿, catálogo real de `LEARNSETS[*].tm`, preço fixo).
- **Pokétickets** (`🎫`, exibidos no topo perto do 💰 Pokédólares e com card próprio no Início): **conta nova já começa com 100** (Cap. leva desta sessão: "Novos jogadores começam com 100 tickets"). Ganha **+50 sozinho a cada 24 horas reais** (mesmo parado, sem lutar — mesmo padrão "preguiçoso"/catch-up da Energia), com um contador regressivo h:mm:ss em tempo real no card. Também dá pra ganhar lutando: **Mestre Especialista dá 5 GARANTIDOS**, sempre, de uma vez só por luta vencida; e um raro **Treinador de Elite** (roster de 6 Pokémon, força de Mestre — ver abaixo) tem chance baixa de largar **até 5**, decrescente quanto mais tickets. **O total NUNCA passa de 1000** — acima disso, QUALQUER ganho novo (diário ou de luta) é descartado, trava no teto ("Acima disso os tickets não contabilizam ou ficam guardados, para em 1000. GASTEM SEUS TICKETS!"). Dá pra trocar **de verdade** por Energia (**1 Pokéticket = +100 Energia**) a qualquer momento — e agora dá pra trocar **VÁRIOS de uma vez** (Cap. leva desta sessão: "colocar uma opção nas áreas que usam energia para carregar múltiplos tickets, caso no máximo 10"): um campo de quantidade (até **10 por vez**, travado ao seu estoque real) aparece não só no card de Pokétickets do Início, mas também na **Área de Captura** e nos **Mestres** (as duas telas que realmente gastam Energia) — sempre lembrando que a Energia **nunca passa de 1000**: se a quantidade pedida encheria além do teto, a troca usa só os Pokétickets REALMENTE necessários pra chegar em 1000 (nunca desperdiça o resto) e avisa que usou menos do que foi pedido. E se uma luta custar mais Energia do que você tem, mas seus Pokétickets cobrem a diferença, um popup 🎫 pergunta se quer usá-los pra completar e entrar em vez de simplesmente recusar a luta.
- **Treinador de Elite** (novo, só a partir da Zona Elite -- Nível 81+): chance rara (12%) de aparecer em vez de um Treinador comum ao entrar numa Zona alta -- roster de 6 Pokémon fortes, no Nível da própria Zona, igual a força de um Mestre Especialista. Não dá Insígnia (não é um Mestre), mas paga mais Pokédólares que um Treinador comum e é a única forma "de rota" de conseguir Pokéticket.
- **Troca de Movimento** (`🔁`, na tela Equipe): troca qualquer Movimento do slot por outro que a Espécie JÁ tem liberado por Nível (custa **10 Energia**) ou por uma **MT** que o Treinador tem em estoque (**grátis** — o custo já foi comprar/ganhar a MT), nunca um Movimento fora do Learnset real da Espécie. "Assim cada Pokémon tem sua própria identidade" — junto com Natureza/Habilidade sorteadas, dois Pokémon da mesma Espécie/Nível podem jogar bem diferente. Subir de Nível **nunca** mexe no kit de Movimentos sozinho (ver "Bug real achado" em Testado, abaixo) — o único jeito de aprender algo novo é passar por aqui.
- **Luta automática**: iniciativa por Velocidade Efetiva, dano pela fórmula acima, log resumido turno a turno. **Gatilho de cura**: com o Pokémon ativo do jogador em ≤20% de PV, a luta pausa e pergunta se quer usar Poção/Superpoção/Hiperpoção (só aparece o que o Treinador realmente tem). Desmaiou e sobra alguém na Equipe? Pausa pra escolher quem entra. **Ganhar XP pode pausar a luta pedindo confirmação de Evolução** (ver acima) -- **como POPUP de prioridade** (Cap. leva desta sessão: "faça com que um popup surja na tela como prioridade e assim o jogador decide se vai evoluir ou não"): tela cheia por cima de tudo (`.ga-overlay`, mesmo componente dos outros popups de prioridade -- Energia insuficiente, Luta difícil, Sequência automática), com o sprite da forma evoluída, nunca mais um card dentro da própria tela de luta que dava pra passar batido rolando o log. **Nunca dá pra desistir** de uma luta já começada. PV é sempre restaurado 100% fora de batalha (não existe dano/condição persistente entre lutas).
- **Energia** (0–1000): Cap. leva desta sessão ("na área de combate vamos começar gastando 5 de energia na área mais fraca e subindo 5 a cada área mais forte, então 5, 10, 15, 20 e assim sucessivamente. Não será mais por nível de pokémon") — entrar na Área de Captura (selvagem, Treinador de Zona OU Treinador de Elite, os 3 tipos que aparecem lá) custa um valor **fixo por Zona**, nunca mais pelo Nível de quem aparece: `5 × (índice da Zona + 1)` — Aprendiz=5, Iniciante=10, Novato=15... até Lendário=50. Desafiar um Mestre Especialista (não pertence a Zona nenhuma) custa um valor **fixo e igual pros 8** (Cap. leva desta sessão: "Os mestres, precisam ter uma entrada uniforme de energia. Todos eles vão custar 300 energias") — `300` de Energia, não importa o Nível do Ace dele (antes escalava por `20 × Nível do Ace`, variando Mestre a Mestre). Recupera `+10` a cada 10 minutos reais — calculado "de forma preguiçosa" (confere quanto tempo passou e credita os blocos de 10 min inteiros), mas agora um `setInterval` de verdade roda a cada 1 segundo em segundo plano e aplica o catch-up **mesmo parado na tela sem clicar em nada** ("a energia não está sendo carregada a cada 10 minutos corrija isso") — antes só acontecia reagindo a uma ação do jogador. O card de Energia do Início também ganhou um **contador regressivo em tempo real** (mm:ss, atualizado a cada segundo direto no DOM, sem re-renderizar a tela toda) no lugar do antigo aviso estático "+10 em X min".
- **Papel de Combate** (`⚔️ Ataque` / `🛡️ Defesa` / `🌿 Suporte` / `🌀 Controle`, na tela Equipe): cada Pokémon pode ser marcado com um papel -- a luta automática passa a escolher o Movimento a usar **por papel**, 60% de chance de ser um Movimento do papel setado, os outros 3 dividem os 40% restantes (~13,3% cada, nunca 100% de um só tipo, e só entre Movimentos com PP sobrando). Todo Movimento cai em pelo menos 1 dos 4 papéis, por regra sobre o texto do efeito (não é uma tabela gigante escrita à mão) -- **e um Movimento PODE valer pra mais de um papel ao mesmo tempo** quando o efeito se encaixa em mais de uma definição (ex.: um golpe de dano com chance de status secundário como Body Slam é `⚔️ Ataque` E `🌀 Controle`): `⚔️ Ataque` foca em eliminar o oponente (todo golpe de dano, Físico/Especial, mais um buff ofensivo puro no próprio usuário sem nenhuma outra tag, tipo Swords Dance); `🛡️ Defesa` foca em se manter vivo (cura o próprio PV, aumenta a própria Defesa/Defesa Especial/Evasão, protege, substituto); `🌿 Suporte` foca em ajudar a si mesmo E aos aliados (cura, melhoria de campo -- clima/terreno/paredes tipo Reflect) -- **inclui curar A SI MESMO, não só aliados**; `🌀 Controle` foca em deixar o oponente com status ruim (Veneno/Paralisia/Sono/Confusão/Queimadura/Atração, reduz Atributo do alvo, prende/impede fuga, armadilha tipo Stealth Rock). O Papel de Combate é **só a camada de seleção** de qual Movimento usar -- a execução é sempre o **efeito OFICIAL de verdade** do golpe escolhido (ver "Usando as regras daqui" acima), nunca mais um efeito genérico por papel.
- **Clima em batalha** (Cap. leva desta sessão: "Quero que traga o clima como no original"): 4 climas reais (🌧️ Chuva / ☀️ Sol Forte / 🏜️ Tempestade de Areia / 🌨️ Granizo) + Neutro (padrão), armados pelos próprios Movimentos oficiais (Rain Dance/Sunny Day/Sandstorm/Hail) por **5 turnos**, sempre com efeito MECÂNICO real na fórmula oficial multiplicativa: Chuva dá ×1,5 de dano pra golpe de Água e ×0,5 pra Fogo (Sol Forte é o inverso); Areia/Granizo causam 1/16 do PV Máximo de dano residual por Rodada em quem não é imune por Tipo (Areia poupa Pedra/Terra/Aço, Granizo poupa Gelo); Moonlight/Synthesis/Morning Sun (os 3 únicos golpes de cura oficialmente sensíveis ao clima) curam 67% no Sol, 25% sob clima "ruim" (Chuva/Areia/Granizo), 50% no Neutro; e ninguém congela sob Sol Forte (regra oficial). Vive por LUTA (`STATE.battle.weather`) — uma luta nova sempre começa em clima Neutro. Aparece como uma etiqueta no topo da tela de luta (ícone + nome + turnos restantes) enquanto ativo.
- **Terreno em batalha** (Cap. leva desta sessão: "Depois disso pode dar continuidade pro lance do terreno"): 4 Terrenos reais (⚡ Elétrico / 🌱 Grama / 🌫️ Névoa / 🔮 Psíquico) + Neutro, armados pelos 4 Movimentos oficiais (Electric/Grassy/Misty/Psychic Terrain) por **5 turnos** — MESMO padrão do Clima acima, e os dois convivem juntos de verdade (nunca um cancela o outro). Efeito mecânico real, mas só em quem está "no chão" (aproximado por não ser Tipo Voador -- Levitate/item ficam pra leva de Habilidades/Itens): Elétrico/Grama/Psíquico dão ×1,3 de dano pro golpe do Tipo correspondente; Névoa dá ×0,5 pro golpe de Dragão; Elétrico impede Sono; Névoa impede Condição persistente; Grama cura 1/16 do PV Máximo por Rodada de quem está no chão. Também aparece como etiqueta na tela de luta.
- **Catálogo de Movimentos** (`🗡️`, novo item no menu lateral -- Cap. "Preciso que crie o catálogo de moves pois não achei"): tabela com os 847 Movimentos do jogo, dado OFICIAL real (Tipo, categoria, Poder/Precisão/PP de verdade -- nunca a escala comprimida do Papel de Combate -- e o efeito em português), com busca por nome e filtro de Tipo/categoria/papel (combináveis entre si).
- **Equipe/Caixa**: até 6 na Equipe, excedente vai pra Caixa; soltar (com confirmação) e puxar da Caixa pra Equipe. **Filtro/busca na Caixa** (Cap. leva desta sessão: "Cria uma área de filtro na caixa dos pokémon pra facilitar encontrar os pokemon que precisamos, um filtro parecido com a pokedex") -- MESMO padrão de filtro da 📕 Pokédex (ver abaixo): busca por apelido, nome da Espécie ou número, dois filtros de Tipo (Tipo 1 e Tipo 2) e um filtro de Papel de Combate (⚔️/🛡️/🌿/🌀) -- todos combináveis entre si, com contador "X de Y Pokémon com esses filtros". Só a Caixa tem filtro (a Equipe nunca passa de 6, não precisa).
- **Salvamento**: Firebase Firestore — **o mesmo projeto que já hospeda o Diário de Treinador** (não é uma conta/projeto novo pra configurar), numa coleção própria (`green_area_saves`) com **1 documento por Treinador**, login por **Nome+PIN** (mesmo modelo do Diário de Treinador: PIN comparado em texto puro no cliente — é uma trava simples "de mesa", não criptografia de verdade). Progresso sincroniza em tempo real (`onSnapshot`) e funciona de qualquer aparelho com o mesmo Nome+PIN. Precisa de **1 passo manual único** no Firebase Console — ver "Habilitar o salvamento online" abaixo.
- **Painel Admin** (`🛡️`, só pra quem loga como "Marcus"): vê TODAS as contas cadastradas (Nome, maior Nível, Insígnias, Energia, Pokédólares), **bane/desbane** (bloqueia login sem apagar o progresso) e **exclui** (some de vez do Firestore, com confirmação em 2 cliques) qualquer conta. Reconhecido pelo ID do documento (o mesmo `saveDocId` do login normal) — mesmo aviso de sempre: é uma trava só no CLIENTE, não uma regra de segurança de verdade no Firestore (mesmo modelo de PIN em texto puro do resto do projeto). Botão **"👁️ Ver Ficha + Log"** em cada conta abre um popup com a **ficha completa** dela (Equipe/Caixa inteiras — Espécie/Nível/Natureza/Habilidade/Papel de Combate de cada Pokémon —, Energia/Pokédólares/Pokétickets/Insígnias, Mochila) e o **log de ações** dela ("Desenvolva um log para o administrador conseguir saber o que cada jogador tá fazendo, todas as ações divididas por jogador"). Cada conta guarda o próprio `STATE.actionLog` (travado nas últimas 500 entradas, mais recente primeiro) e registra toda ação de JOGO com efeito de verdade — entrar/sair da conta, capturar (sucesso ou fuga), lutar (início e vitória/derrota), evoluir (aceitar ou recusar), comprar (Loja/MT), trocar Movimento/Papel de Combate/apelido, usar item em luta, mover Pokémon Equipe↔Caixa, soltar Pokémon, trocar Pokéticket por Energia, banir/desbanir/excluir (fica no log do PRÓPRIO Admin, não da conta alvo) e toda ferramenta de debug. Navegação pura de UI (abrir/fechar popup, trocar de aba, digitar numa busca) fica de fora de propósito — não é uma ação, é só olhar a tela.
- **`🛠️ Ferramentas de Teste`**: encher Energia, +1 Pokéticket (+100 Energia), +50 Pokétickets, +itens (Pokébolas dos 4 tipos + Poções), +500 Pokédólares, resetar o save — tudo marcado como debug, nunca escondido como mecânica real.
- **Dicas passando o mouse (`title`)**: Energia, Pokédólares, Pokétickets, Insígnias e Pokédex no topo explicam a própria mecânica ao passar o mouse (o que é, como ganha/gasta, o que fazer) — "quando eu descansar o mouse em cima de algum lugar... em energia, ele explica o que é e me explica o tempo que enche, o que usar pra encher".
- **📖 Wiki** (nova aba): a mesma explicação de cada mecânica (Energia, Zonas, XP/Evolução, Pokébolas, Loja/Pokédólares/Pokétickets, Mestres, Troca de Movimento/MT, Natureza/Habilidade, Pokédex) reunida numa tela só, sem precisar ficar passando o mouse em cada estatística — "cria também uma aba onde fica a Wiki do jogo".
- **Busca e filtros na Pokédex**: campo de busca por Nome ou Número, filtro de Status (Todos / Capturados / Não capturados) e dois filtros de Tipo (Tipo 1 e Tipo 2, combináveis entre si e com os outros) — "coloca uma busca na pokédex pra facilitar encontrar pokémon".
- **Layout com menu lateral** (inspirado no site the crims, MESMA paleta de cores de sempre — só o layout mudou): a antiga barra horizontal de navegação virou uma **barra lateral fixa** (marca no topo, estatísticas rápidas, lista de navegação vertical ícone+rótulo, Sair fixo embaixo), igual à referência enviada pelo usuário. No celular (tela ≤760px) a barra vira uma **gaveta off-canvas** — escondida por padrão, abre deslizando com o botão `☰` de uma barrinha de topo compacta (marca + Energia sempre visíveis), fecha sozinha ao navegar pra qualquer tela.
- **🏥 Centro Pokémon** (Cap. leva desta sessão -- Fase 1 de 4: "Quero criar uma área, chamada Centro Pokémon. Ali os jogadores vão poder... trocar chat e se desafiarem para um pvp... O chat é livre para todos verem o que todos falam... pedido de amizade entre os jogadores"): área social nova, item próprio no menu lateral (com um selo vermelho mostrando quantos pedidos de amizade estão pendentes, visível de QUALQUER tela). **Diretório de Treinadores POR PRESENÇA AO VIVO** (Cap. leva de correção desta sessão: "só quero que apareçam os treinadores que estiverem no menu do Centro pokémon, ao sair ele sai dali também") -- mostra só quem está com ESSA tela aberta agora (nunca a própria conta): entrar grava um "ping" (`green_area_centro_presenca`) e renova sozinho a cada 25s enquanto a tela fica aberta; sair da tela, sair da conta ou fechar a aba (via `beforeunload`) apaga o ping na hora, e qualquer ping mais velho que 75s (3× o intervalo -- aba travada/fechada sem avisar) é filtrado como "fantasma" no cliente, já que não existe limpeza automática do lado do servidor num app 100% estático sem backend. **Chat público ao vivo** (visível a TODOS que estiverem na tela -- histórico travado nas últimas 200 mensagens, com trim automático quando passa do teto) e **pedido/aceite/recusa de amizade** (chega de verdade no inbox do outro Treinador mesmo que ele não esteja online no momento -- o pedido fica esperando, visível desde o LOGIN dele, não só quando ele entra no Centro Pokémon). Desfazer amizade também disponível. Arquitetura pensada pra nunca perder dado com 2 jogadores mexendo ao mesmo tempo: as 4 coleções novas (`green_area_chat_publico`/`green_area_inbox`/`green_area_amizades`/`green_area_centro_presenca`) vivem TOTALMENTE fora do `state` salvo de cada conta -- escrever no documento de OUTRO jogador (mandar uma mensagem, um pedido de amizade) sempre usa `FieldValue.arrayUnion`/`arrayRemove` (operação atômica do lado do servidor), nunca "lê tudo, edita, escreve tudo de volta" (que se atropelaria se o dono daquela conta salvasse alguma coisa no meio do caminho, já que o save de cada um reescreve o campo `state` inteiro de uma vez); a presença é a única escrita social sempre feita no PRÓPRIO documento (nunca no de outro jogador), então nem precisa de `arrayUnion` -- `set()` direto já é seguro. Erro de rede/permissão em qualquer ação social (mandar pedido, mandar mensagem, carregar diretório/inbox/amizades) agora mostra o **motivo real devolvido pelo Firestore** na tela, não mais um "verifique sua internet" genérico -- ajuda a identificar na hora se é falta de internet mesmo ou as regras de segurança do Firestore ainda não autorizando a coleção (ver "Habilitar o salvamento online" acima). **Troca de Pokémon/itens/dinheiro e desafio de PvP ainda não existem** -- avisado na própria tela (não escondido), chegam nas próximas fases desta mesma leva.
- **💬 Mensagens** (Cap. leva desta sessão -- Fase 2 de 4: "Crie também um chat privado (que pode ser acessado de qualquer área)"): item PRÓPRIO no menu lateral, com o mesmo tipo de selo vermelho do Centro Pokémon (agora contando mensagens privadas NÃO LIDAS, somadas de todas as conversas) -- **acende de QUALQUER tela**, não só dentro da própria tela de Mensagens, porque o listener de `green_area_dm` roda em segundo plano desde o LOGIN (mesmo padrão "independente da tela" já usado pro inbox de pedido de amizade, o contador de Energia e a sequência automática de lutas). Só dá pra conversar com quem já é **amigo** -- lista uma linha por amigo (prévia da última mensagem + selo de não-lidas daquela conversa, mais recente primeiro), clicar abre a conversa (também dá pra abrir direto pelo botão "💬 Mensagem" na lista de amigos do 🏥 Centro Pokémon). Histórico próprio POR PAR de Treinadores (`green_area_dm/{par}`, mesmo id de par ordenado que `green_area_amizades` usa, mas numa coleção TOTALMENTE separada), travado nas últimas 200 mensagens por conversa (trim automático, mesmo padrão do chat público) e mensagem individual cortada em 300 caracteres. Mensagens sempre via `FieldValue.arrayUnion` (os 2 lados mandando ao mesmo tempo nunca se atropelam -- mesma arquitetura do chat público e do pedido de amizade). "Lido até quando" fica guardado no PRÓPRIO save de cada Treinador (`STATE.dmLidoAte`, nunca no documento compartilhado da conversa) -- abrir uma conversa marca na hora como lida, e uma mensagem nova chegando enquanto ela já está aberta na tela também marca como lida na hora (nunca fica um selo aceso numa conversa que o jogador já está literalmente olhando).
- **🔄 Troca ao vivo** (Cap. leva desta sessão -- Fase 3 de 4: "trocar pokémons, itens, dinheiro" -- confirmado com o usuário: troca AO VIVO, os 2 online juntos montando a oferta em tempo real, não uma caixa de correio): botão "🔄 Trocar" na lista de amigos do 🏥 Centro Pokémon (só entre amigos, mesma trava do chat privado) abre uma tela de troca com **1 documento próprio por troca** (`green_area_trocas/{id}`, id aleatório -- o MESMO par pode trocar várias vezes ao longo do tempo, cada uma um documento novo). Cada lado monta a PRÓPRIA oferta (Pokédólares, Pokébolas/Poções e Pokémon **da Caixa** -- nunca da Equipe, de propósito, pra nunca esvaziar o time de ninguém no meio de uma troca) e vê a oferta do outro **ao vivo**, mesmo sem estar na tela de Troca (listener roda desde o login, igual o chat privado). Mudar a própria oferta cancela as 2 confirmações de novo -- a troca só executa quando os DOIS confirmarem a versão final. Quando os 2 confirmam, **cada cliente aplica a PRÓPRIA metade sozinho** (tira o que ofereceu, dá o que recebeu, salva no PRÓPRIO save) -- nenhum dos lados nunca escreve no `green_area_saves` de ninguém mais (mesma arquitetura de segurança do resto do Centro Pokémon). Cancelar só é possível ANTES de qualquer lado ter aplicado a própria metade.



## Simplificações deste protótipo (documentadas de propósito)

- **Natureza afeta Atributo Efetivo de verdade (multiplicador oficial ×1,1/×0,9); Habilidade é só identidade** (ainda não simulada em batalha — sem gatilhos tipo Intimidate/Levitate). Estágio (-6..+6) e Condição (Queimadura/Veneno/Paralisia/Sono/Congelamento/Confusão) já são reais em luta (ver "Usando as regras daqui" acima) — falta só Clima, Terreno, Item segurado e gatilho de Habilidade em combate, ver "Em aberto".
- **Sem IV/EV** — fixos em 31 (perfeito)/0 pra todo Pokémon, documentado de propósito (fora do escopo desta leva, que era sobre fidelidade de COMBATE, não a metagame inteira de treino de Atributo).
- **PP real, mas sem "PP Up"** — PP máximo é sempre o oficial puro (`MOVES_OFICIAL[*].pp`), sem item/mecânica pra aumentar o teto.
- **Kit de Movimentos**: os 4 mais altos do Learnset por Nível na captura, mas **subir de Nível sozinho nunca muda o kit** (ver "Bug real achado" em Testado) — customiza depois via 🔁 Troca de Movimento (Nível liberado = 10 Energia, MT em estoque = grátis), nunca um Movimento fora do Learnset real da Espécie (nem por Nível nem por MT).
- **`nivelTipico` (Zonas/captura) é uma aproximação documentada**: o Bulbapedia não tem um dado genérico de "em que Nível cada Pokémon aparece" (só por jogo/rota específica) — a aproximação usada aqui combina Estágio de evolução + Total de Atributos Base (BST), os dois sinais reais que a base já tinha, exatamente o mesmo par que o design oficial de rotas usa na prática.
- **Chance de captura agora usa dado real** (não é mais aproximação por Estágio): `CAPTURE_RATE`, a Taxa de Captura oficial de cada Espécie (3–255, extraída do próprio repositório da PokeAPI) dividida por 255, vezes o multiplicador da bola escolhida — a mesma estimativa simples que o próprio Bulbapedia cita pra um alvo com PV cheio e sem status (sempre o caso aqui, a bola só é oferecida depois de VENCER).
- **Evolução por Nível usa `nivelTipico` como limiar aproximado quando o gatilho oficial não é por Nível** (pedra, troca, felicidade — este protótipo não tem esses itens/mecânicas). Quando há mais de uma evolução possível pra mesma Espécie (Eevee etc.), sorteia 1 entre as já elegíveis — mesmo "sempre aleatório, respeitando a limitação" de Natureza/Habilidade.
- **PIN em texto puro** (mesma escolha de design do Diário de Treinador) — é uma trava simples "de mesa" contra digitar o Nome de outro Treinador por engano, não é autenticação de verdade; não use um PIN sensível. O Painel Admin usa o mesmo modelo (reconhecido pelo Nome da conta, sem regra de segurança real no Firestore).
- **Progressão de Insígnia sempre sequencial** (não dá pra escolher a ordem, diferente dos jogos oficiais que às vezes liberam ordem livre numa região).
- **Treinador aleatório de Zona é sempre genérico** (nome de uma lista curta, 1-2 Pokémon sorteados de uma piscina mais ampla que a selvagem da própria Zona) — não tem elenco fixo curado como os 8 Mestres.

## Testado

Suíte própria (fora do repositório, no scratchpad da sessão que criou isto — ver o histórico do commit), com um stub de Firestore em memória (imita `get`/`set`/`update`/`delete`/`onSnapshot`/consulta da coleção inteira do SDK real, sem precisar de rede) pra testar a persistência sem depender do banco de verdade: criação de conta (inicial sempre um dos 27 de verdade, documento gravado no Firestore); nome duplicado recusado; PIN incorreto/Treinador inexistente/conta banida recusados; `mutate()` salva no Firestore depois do debounce; sair/entrar de novo recupera o progresso salvo; migração de um save local legado pra uma conta online; `DT.gaDebugReset` reseta o documento da conta existente; Energia nunca passa de 1000 mesmo com muito tempo simulado; `pickMovesForLevel` varrida em ~150 espécies × 5 Níveis, nunca devolve um kit sem nenhum Movimento de dano; motor de batalha selvagem resolvido até o fim nos dois desfechos (vitória E derrota, não é vitória garantida); custo de Energia descontado de verdade; trava contra 2 lutas simultâneas; bloqueio por Energia insuficiente sem descontar nada; progressão sequencial de Mestre recusando pular; fila completa dos 8 Mestres vencida do 1º ao último (incluindo troca por desmaio de verdade); todas as telas (incluindo Loja/Admin) + a tela de login + popup da Pokédex (não visto/visto/capturado) renderizando sem estourar em vários estados; **stress test de 400 ciclos de luta aleatórios** (selvagem/Treinador de Zona/Mestre, Níveis 1–16, decisões de item/troca aleatórias, já logado via Firestore stub) — 0 estouros, 0 lutas travadas. **5 arquivos de teste** agora (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`/`test_social.js`, este último novo -- ver "Centro Pokémon" no changelog abaixo), todos importando o MESMO stub de Firestore (`firestore_stub.js`, extraído da cópia colada em cada um) em vez de 4 versões independentes que podiam divergir sem ninguém notar.

**Leva de Nível/Zonas/Loja/Admin**: sistema de level up (4 vitórias = +1 Nível, contador zera, trava exatamente no teto sem estourar, aprende Movimento novo de verdade); as 6 Zonas nomeadas existem com a faixa certa (sem buraco nem sobreposição entre elas), nunca sorteiam espécie acima do Estágio de evolução da Zona, e recusam entrar quando o líder não chegou no Nível mínimo (com o aviso certo); recompensa em Pokédólares confirmada MAIOR contra Treinador que contra selvagem no mesmo Nível; comprar na Loja debita/credita certo e recusa sem saldo. Painel Admin testado com **cliques reais** (mesmo harness `test_dom_globals.js` que carrega o `<script>` sem remover o invólucro): criar a conta "Marcus" de propósito libera a aba 🛡️ Admin, o painel lista as contas de verdade (via `get()` na coleção inteira do Firestore stub), Banir/Desbanir mudam o estado na tela, e Excluir pede confirmação em 2 cliques antes de apagar o documento de vez.

**Leva de Nível 1-100/Natureza/Habilidade/Troca de Movimento/MT** (esta sessão): teto revertido pra 100 e Zonas/Ace dos Mestres reescalonados de volta pra fórmula original, confirmados por teste; todo Pokémon nasce com Natureza real (`NATURES_DEX`) e Habilidade real E LEGAL da própria Espécie (`SPECIES_DEX[*].hab`); Troca de Movimento cobrando exatamente 10 Energia pra Movimento de Nível, grátis (mas consumindo 1 MT do estoque) pra Movimento de MT, e recusando Movimento fora do Learnset da Espécie; catálogo de MTs real (`LEARNSETS[*].tm`, >250 Movimentos) comprável na Loja, debitando/creditando certo; Mestre vencido soltando 1 MT de graça do próprio elenco.

**Bug real achado durante o teste** (não pelo usuário desta vez, pego ANTES de publicar): ao reverter o Nível dos Mestres pra fórmula original (Ace 15, não mais 2), a luta contra o Mestre Enxame passou a ser disputada de verdade em vez de vitória garantida — e isso expôs que `DT.gaUsarItemCura(itemKey)` silenciosamente NÃO FAZIA NADA (nem curava, nem fechava a pergunta) quando chamado com um item que o Treinador não tinha em estoque, deixando a luta travada pra sempre esperando uma resposta que nunca vinha. A UI de verdade nunca mostra botão pra item que não existe no estoque, então isso nunca apareceu jogando normalmente — só foi alcançável testando o motor direto. Corrigido: item inválido/sem estoque agora cai no mesmo caminho de "recusar cura" (nunca mais fica esperando pra sempre).

**Leva de Zonas/XP/Evolução/Pokébolas/Mestres/Pokédex/Pokétickets** (esta sessão): as 10 Zonas cobrem 1–100 sem buraco nem sobreposição entre elas, nenhuma fica com a piscina selvagem vazia, nenhuma sorteia Lendário/Mítico, e o próprio caso citado no pedido é verificado de verdade (Golem só aparece a partir de uma Zona alta, nunca 1-10); XP por Faixa confirmado nos 3 casos (mesma Faixa/vizinha = 1 XP, 2+ Faixas acima = XP igual à diferença, 2+ Faixas abaixo = 0 XP), 10 XP fecha 1 Nível, XP grande de uma vez sobe vários Níveis corretamente; Evolução testada numa luta selvagem DE VERDADE (não um objeto fake) — pausa pedindo confirmação, recusar preserva Espécie/Movimentos e pergunta de novo no próximo Nível elegível, aceitar troca a Espécie preservando Movimentos e Natureza e marca a forma nova como capturada; as 4 Pokébolas confirmadas com chance de captura real (Ultra Ball > Pokébola pra mesma Espécie, usando `CAPTURE_RATE` de verdade); os 8 Mestres confirmados com 5 Pokémon reais e não-lendários cada, Ace na fórmula nova (25..95), recompensa em Pokédólares/Pokétickets somada por oponente derrotado (Mestre > Treinador > selvagem no mesmo Nível); popup "Ver Time" e da Pokédex (área/Movimentos possíveis com dado real) renderizando sem estourar; migração de uma conta salva ANTES desta leva confirmada (pokebolas número vira objeto com as 4 chaves, `xpNivel` nasce do zero). Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) passando sem regressão, `test.js` rodado 4x seguidas pra descartar flake de RNG.

**Bug real achado durante o teste** (não pelo usuário desta vez, achado ANTES de publicar, ao reescrever o motor de level up pra virar "por XP e no meio da luta"): o código antigo recomputava o kit de 4 Movimentos inteiro (`pickMovesForLevel`) TODA VEZ que o Pokémon subia de Nível, de graça — o que contradizia o próprio pedido que criou a Troca de Movimento ("moves que já são do nível... trocar gastando 10 energia por move", ou seja, NENHUMA troca deveria ser de graça) e, pior, apagava silenciosamente qualquer Movimento de MT que o Treinador tivesse ensinado à mão. Como o level up agora acontece com muito mais frequência (a cada oponente derrotado, não só a cada 4 vitórias inteiras), esse bug ia ficar bem mais visível/incômodo. Corrigido: subir de Nível nunca mais mexe no kit de Movimentos — o único jeito de aprender algo novo continua sendo a tela 🔁 Troca de Movimento (Nível = 10 Energia, MT = grátis).

**Bug real achado pelo usuário** ("cliquei e não aconteceu nada, nem muda de aba"): `DT` (e, em 4 botões específicos, `SESSION`/`render` direto) só existiam DENTRO da closure do `<script>`, nunca em `window` — e todo `onclick="..."` inline roda em escopo GLOBAL do navegador, não enxerga variável nenhuma de dentro de uma função anônima. Resultado: **nenhum botão jamais funcionou de verdade num navegador**, desde o primeiro commit deste protótipo — os testes acima nunca pegaram isso porque removem esse "invólucro" antes de rodar (de propósito, pra inspecionar `STATE`/`DT` direto), o que mascara exatamente essa classe de bug. Corrigido com `window.DT = DT;` (mesmo padrão que o Diário de Treinador já usava) + os 4 botões reescritos como `DT.*` (nunca `SESSION`/`render()` cru num onclick). Ganhou um harness novo (`test_dom_globals.js`) que carrega o `<script>` **sem remover o invólucro** — do jeito que o navegador carrega de verdade — dentro de um contexto `vm` isolado, faz uma varredura estática de todo `onclick="..."` renderizado (confirmando que nenhum identificador livre escapa da lista de globais reais) e ainda executa de verdade um punhado dos cliques mais representativos (Criar novo → preencher → Começar a jornada; Cancelar do reset; banner de migração) nesse mesmo escopo global separado — reproduzindo o clique exatamente como um usuário faria.

**Leva de Zonas liberadas/6 Mestres variados/Energia+Pokétickets/discrepância/Pokédex com filtro/ícones pixelados** (esta sessão): entrar numa Zona nunca mais trava por Nível (testado com o time inteiro no Nível 1 contra a Zona Lendário); os 8 Mestres confirmados com 6 Pokémon cada, todos de família evolutiva diferente entre si (inclusive comparando entre Mestres diferentes); trocar Pokéticket por Energia debita/credita certo e recusa sem estoque; luta cara demais pra Energia atual, mas coberta por Pokétickets suficientes, pausa perguntando em vez de recusar direto, e confirmar de fato usa os Pokétickets e libera a luta; popup de luta muito discrepante (Faixa do oponente 2+ acima da Faixa do MELHOR Pokémon do time, não só do líder escolhido) pausa antes de gastar Energia, e Cancelar não desconta nada; os 4 filtros da Pokédex (busca por nome/número, status de captura, Tipo 1, Tipo 2) testados isolados e combinados, batendo com a contagem real. Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de Energia em tempo real/Pokétickets reformulados/Papel de Combate/menu lateral** (esta sessão): `tickEnergy()` chamado sozinho (sem `mutate()`/ação do jogador) já aplica o catch-up de blocos de 10min de verdade, e `secondsToNextEnergyTick()`/`formatMMSS` confirmados batendo com o tempo restante certo; Pokétickets confirmados ZERO contra selvagem e Treinador comum, Mestre sempre 5 garantidos (mesmo com 6 Pokémon no time, não soma por Pokémon), Treinador de Elite testado com amostra de 4000 rolagens confirmando 0 é o resultado mais comum e a chance cai de verdade conforme mais tickets (1 > 2 > 3 > 4 > 5), e o roster de Elite confirmado com 6 Pokémon no Nível da própria Zona; Papel de Combate testado nos 3 papéis com 3000 amostras cada, confirmando ~60% pro papel setado e ~20%/20% pros outros dois (nunca 100%), TODOS os 847 Movimentos caindo num papel válido, papel sem Movimento disponível no kit redistribuindo sem travar, e o efeito mecânico de verdade (`🛡️ Defender` cura o próprio PV, `🌀 Suporte` tira PV do alvo) confirmado chamando o motor de batalha direto; menu lateral testado abrindo/fechando via `DT.gaToggleSidebar` (classe `open` + fundo escurecido aparecendo/sumindo de verdade no HTML) e fechando sozinho ao navegar (`DT.gaGoto`); layout responsivo conferido visualmente em capturas de tela reais (Chrome headless, viewport emulado via CDP) em desktop (1280px) e celular (390px) nas duas situações (gaveta fechada/aberta). Suíte completa (`test.js`/`test_firebase.js`/`stress.js`, este último rodado com o papel de combate sorteado aleatoriamente pra exercitar os 3 caminhos novos/`test_dom_globals.js`) sem regressão.

**Lore em pt-BR completa** (esta sessão, conclusão): `LORE_PT_DEX` confirmado com as 1025 entradas, uma por Espécie, sem nenhum buraco entre 1 e 1025 e sem nenhuma entrada vazia/curta demais -- traduzida à mão, em lotes, a partir do texto oficial em inglês da própria Pokédex (o mesmo conteúdo do Bulbapedia). Suíte completa sem regressão.

**Leva de ajustes finos no menu lateral** (esta sessão): corrigido um bug real introduzido na leva anterior -- `overflow-x:hidden` em `html,body` (adicionado como proteção contra estouro horizontal no celular) quebrava por completo o `position:sticky` da barra lateral, porque QUALQUER ancestral entre um elemento sticky e o viewport com overflow diferente de "visible" vira um novo "scroll container" pro navegador, e o sticky passa a grudar relativo a esse container (que nunca rola de verdade) em vez de rolar junto com a página inteira -- na prática a barra lateral sumia ao rolar. A proteção contra estouro horizontal ficou só em `.ga-main` agora (nunca em ancestrais da barra lateral) -- confirmado com captura de tela real via Chrome DevTools Protocol, rolando a página 1800px: `getBoundingClientRect()` da barra lateral confirma `top:0` antes e depois de rolar. Também: `.ga-body` perdeu o teto artificial de 900px (agora 1600px, "pegar quase toda a tela") e os cards de Pokémon (`.ga-poke-card`) viraram flex column com as ações (`Escolher`/`Liderando` etc.) empurradas pro fundo via `margin-top:auto`, alinhando o botão na MESMA base entre cards vizinhos mesmo quando o conteúdo acima varia de altura (ex.: 1 Tipo vs. 2 Tipos) -- confirmado visualmente numa grade de 6 Pokémon com quantidades de Tipo diferentes. Suíte completa (`test.js`/`test_firebase.js`/`test_dom_globals.js`) sem regressão.

**Leva de cards alinhados/Pokéticket com card próprio** (esta sessão): a mesma técnica CSS já usada nos cards de Pokémon (flex column + botão final empurrado pro fundo via `margin-top:auto`, com `align-self:center` só no botão pra não esticar linhas internas tipo os ícones da Mochila) foi estendida pros outros 3 tipos de card do site — `.ga-stat-card` (Início: Energia/Pokétickets/Mochila/Insígnias), `.ga-zona-card` (Área de Captura) e `.ga-loja-card` (Loja) — "isso tem que ser feito em todos os cards. De preferência centralizando". Confirmado visualmente via captura de tela real (CDP) nas 3 telas, com botões (`Entrar na mata`, comprar, trocar Pokéticket) sempre na mesma base entre cards vizinhos, mesmo com conteúdo de altura diferente acima. Também: **Pokétickets ganhou card próprio** no Início (antes só um número pequeno dentro do card de Energia) mostrando a contagem em destaque igual aos outros stats, e o botão "Trocar 1 por +100 de Energia" saiu de dentro do card de Energia pra virar um botão independente dentro do novo card de Pokétickets — "Adicione também o contador nos tickets, não tá aparecendo... Crie um botão independente pra recarregarmos a energia com ticket". Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de Papel de Combate com 4 categorias (Ataque/Defesa/Suporte/Controle)** (esta sessão): substitui o sistema antigo de 3 papéis EXCLUSIVOS por 4, onde um Movimento pode carregar mais de 1 tag ao mesmo tempo -- "Categorize as habilidades nessas 4 categorias pra facilitar e pode categorizar uma habilidade em mais de uma categoria quando isso aplicar". Confirmado: `Tackle`/`Absorb` (dano) são `ataque`; `Recover`/`Roost`/`Synthesis`/`Rest`/`Protect`/`Substitute`/`Iron Defense`/`Calm Mind`/`Acid Armor` (sobrevivência própria) são `defesa`; `Reflect`/`Light Screen` (parede pro time todo, não só quem usou) viraram `suporte` (antes eram `defender` no sistema de 3); `Toxic`/`Thunder Wave`/`Sleep Powder`/`Stealth Rock`/`Growl`/`Taunt`/`Attract` (afligem o oponente) são `controle`; `Swords Dance`/`Agility` (buff ofensivo puro, sem outra tag) caem em `ataque`; `Body Slam` (dano físico com chance de paralisar) confirmado com AS DUAS tags `ataque` E `controle` ao mesmo tempo, mas a execução continua sendo sempre a fórmula de dano normal (nunca o efeito de Status) -- todos os 847 Movimentos confirmados caindo em pelo menos 1 papel válido. Sorteio ponderado confirmado nos 4 papéis (~60% pro setado, os outros bem menos, nunca 100%); papel sem golpe disponível no kit redistribui sem travar. Efeito de verdade em luta confirmado nos 4 casos -- `defesa` cura o próprio PV, `suporte` cura o TIME INTEIRO (Aromatherapy testado curando um 2º Pokémon da Equipe que nunca tinha entrado em campo naquela luta), `controle` tira PV do alvo, `ataque`-sem-outra-tag (Swords Dance) usa o placeholder e cura o próprio PV. Migração de save antigo confirmada (`atacar`->`ataque`, `defender`->`defesa`, `suporte` antigo segue `suporte`). **Bug real achado durante o teste** (não pelo usuário desta vez, achado ANTES de publicar): dividir os 40% que sobram do papel setado em 3 fatias de ~13,3% (era 20%/20% nos 2 papéis não-setados do sistema antigo de 3) deixa uma luta entre Pokémon focados em `defesa`/`suporte`/`controle` picar um golpe de dano de verdade bem menos vezes por rodada -- e o `guard` de segurança de `runBattleUntilPause` (força um resultado justo depois de rodar rodadas demais, "nunca trava a luta") era uma variável LOCAL que reiniciava do zero em toda chamada nova da função, e como a função sempre RETORNA numa pausa (cura/troca/evolução) e é chamada de novo depois, uma luta que pausa muitas vezes nunca acumulava rodada nenhuma nesse guard -- podendo, em teoria, rodar pra sempre sem nunca forçar o resultado justo. Corrigido: o guard agora vive em `STATE.battle.roundGuard` (sobrevive a pausas, nunca reinicia sozinho) -- achado rodando o `stress.js` repetidas vezes (intermitente, ~1 em cada 3-5 rodadas de 400 ciclos antes da correção), confirmado 0 lutas travadas em 10 rodadas seguidas depois de corrigido. Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de Pokétickets: grant diário de 50 + teto de 1000** (esta sessão): `tickPoketickets()` (mesmo padrão preguiçoso/catch-up do `tickEnergy()`) confirmado aplicando os blocos de 24h que já passaram mesmo chamado sozinho, sem `mutate()`/ação do jogador; travando em 1000 mesmo com MUITOS dias acumulados de uma vez (ex.: conta abandonada por 30 dias) -- nunca estoura o teto num salto só; `secondsToNextPoketicketTick()` zera certo no teto; o mesmo timer de 1s que já cuidava da Energia (`energyTickLoop`) agora também chama `tickPoketickets()` e atualiza um contador regressivo h:mm:ss em tempo real (`formatHHMMSS`) no card de Pokétickets do Início. O teto de 1000 confirmado valendo em QUALQUER fonte de ganho -- recompensa de batalha (`finishBattle`) e a ferramenta de debug `+50 Pokétickets` também travam, nunca passam. Migração de conta antiga testada (`poketicketsUpdatedAt` nasce agora pra quem não tinha, sem tentar "recuperar" dias que passaram antes do campo existir; conta que já tinha mais de 1000 -- impossível antes desta leva -- trava em 1000 na migração também). Suíte completa sem regressão.

**Leva de Ficha + Log de ações do Admin** (esta sessão): `logAction()` confirmado empilhando entrada de verdade em `STATE.actionLog` (tipo/desc/timestamp), travando em `ACTION_LOG_MAX` (500) e descartando as mais ANTIGAS primeiro quando estoura; uma amostra representativa de ações de jogo (`gaDebugPokedolares`/`gaComprarItem`/`gaSetPapelCombate`/`gaMandarPraCaixa`) confirmada chamando `logAction()` de verdade, com o `tipo` certo, na hora certa; login de verdade confirmado logando "Entrou na conta." sozinho (hook único em `attachSaveListener`, cobre Entrar/Criar/Migrar/sessão salva recuperada ao recarregar, sem duplicar em nenhum). Popup "👁️ Ver Ficha + Log" testado com **cliques reais** (mesmo harness `test_dom_globals.js`): abre de verdade mostrando o docId certo no título, a seção de Equipe da conta e o log com a entrada real de login; fechar tira o popup da tela de verdade. Confirmado visualmente (Chrome headless via CDP) com uma ficha simulada (3 Pokémon + 5 entradas de log) — layout largo (`.ga-overlay-card.wide`, 900px) cabendo a grade de Pokémon e o log rolável sem cortar nada. Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de motor de combate fiel ao jogo oficial (Bulbapedia/PokeAPI)** (esta sessão): "Então vamos usar o sistema de combate baseado no original, pegando informações do bulbapedia. Vamos manter as roles mas quero um sistema de combate bem fiel." Trocou por completo o motor de combate (antes uma adaptação de mesa do RPG principal — d10, Tipo somando/subtraindo) pelo oficial de verdade: Atributo pela fórmula real (`officialStat`, IV 31/EV 0 fixos e documentados, Natureza ×1,1/×0,9 real -- confirmado batendo com a tabela pública de Stats de Charizard Nv.50/Nv.100); Tipo multiplicativo real (`typeEffMultiplier`, confirmado ×2 Fogo→Grama, ×0 Elétrico→Terra e ×0 Normal→Fantasma); dano pela fórmula oficial (`officialDamage`); Crítico 1/24 base por Estágio (`officialCritRoll`, não mais d10); Acerto por porcentagem real com Estágio de Precisão/Evasão (`officialAccuracyRoll`); Estágio -6..+6 pra todo Atributo (`bumpStage`/`stageMultiplier`, vive só no battler); Condição persistente oficial (Queimadura/Veneno/Veneno Grave/Paralisia/Sono/Congelamento) + Confusão volátil separada, com as imunidades de Tipo certas (Aço/Venenoso nunca envenena, Fogo nunca queima, Elétrico nunca paralisa, Gelo nunca congela); PP real por Movimento (`MOVES_OFICIAL`, buscado via PokeAPI pros 847 Movimentos) consumido de verdade, caindo pra Struggle quando acaba; golpes de N hits com a distribuição oficial 35/35/15/15; dreno/recuo real (% do dano). O efeito de execução de todo Movimento agora é sempre o OFICIAL de verdade (motor genérico orientado a dados), nunca mais um placeholder por papel -- `curarTimeInteiro` e os efeitos genéricos por papel (`STATUS_DEFESA_CURA_PCT` etc.) foram REMOVIDOS por completo; o Papel de Combate (Ataque/Defesa/Suporte/Controle) virou só a camada de SELEÇÃO (`pickMoveByRole`, agora também filtrando por PP sobrando). Confirmado em luta de verdade: `defesa` (Recover) cura o próprio PV com o healing% oficial (50%); `suporte` (Life Dew) também cura o próprio PV (o healing% oficial sempre cura quem usou o golpe -- em 1x1 não existe aliado fora de campo pra receber a parte "e aliados" do efeito, então na prática vira self-heal, coerente com a correção do papel Suporte "cura a si mesmo TAMBÉM"); `controle` (Growl) NÃO causa dano (é Status) e reduz de verdade o Estágio de Ataque do alvo; `ataque` sem outra tag (Swords Dance) aplica de verdade +2 Estágio de Ataque (não mais nenhum placeholder de cura). `stress.js` rodado com 400 lutas reais (papel de combate sorteado, PP real se esgotando, Struggle entrando em ação, Estágio/Condição reais acontecendo) -- 0 crashes, 0 lutas travadas. Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de Clima + custo de Energia por Zona + XP por categoria** (esta sessão): "Quero que traga o clima como no original. Então vamos fazer dessa forma. E outra coisa, sobre o gasto de energia... E a experiência ganha agora vai ser a seguinte..." Três mudanças independentes, todas testadas: **(1) Clima** -- `weatherDamageMultiplier` confirmado ×1,5 de dano pra golpe de Água na Chuva/Fogo no Sol, ×0,5 pro Tipo oposto, ×1 pra Tipo não afetado; Rain Dance de verdade arma `STATE.battle.weather={tipo:'chuva',turnsLeft:5}` numa luta real; Water Gun (Água) causou mais dano de verdade na Chuva do que no Neutro (fórmula oficial completa, não só o multiplicador isolado); `applyWeatherResidual` confirmado causando dano real em Pidgey (Normal/Voador) sob Tempestade de Areia mas NÃO em Geodude (Pedra/Terra, imune), com a contagem de turnos decrescendo de verdade e revertendo sozinha pro Neutro quando chega a 0; Moonlight confirmado curando mais PV de verdade sob Sol Forte (67%) do que sob Chuva (25%); Congelamento confirmado NUNCA acontecendo sob Sol Forte, mesmo num alvo sem imunidade de Tipo. **(2) Custo de Energia por Zona** -- `custoEnergiaZona(idx)` confirmado em 5/10/25/50 pras Zonas 0/1/4/9; uma luta real via `DT.gaEntrarCaptura` na Zona Aprendiz confirmada gastando exatamente 5 de Energia, independente do Nível de quem apareceu (selvagem ou Treinador de Zona). **(3) XP por categoria** -- `xpParaVitoria` confirmado retornando 2 XP na própria Faixa, 1 XP uma Faixa abaixo, 0 XP 2+ Faixas abaixo, e 3/4/5 XP pra 1/2/3 Faixas acima (2 de base + 1 por Faixa acima). Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva do Catálogo de Movimentos** (esta sessão): "Preciso que crie o catálogo de moves pois não achei. Onde está?" -- tela nova (`🗡️ Movimentos`, item novo no menu lateral) listando os 847 Movimentos com dado OFICIAL real. Confirmado: sem filtro nenhum a lista tem os 847 Movimentos de verdade; busca por nome ("recover") acha exatamente o Movimento certo; filtro de Tipo ("Fogo") só devolve Movimentos daquele Tipo; filtro de categoria ("Status") só devolve golpes de Status; filtro de papel ("controle") só devolve Movimentos com aquela tag; filtros combinados (Tipo + categoria ao mesmo tempo) funcionam juntos, sem um sobrescrever o outro; a tabela renderiza com Poder/Precisão/PP REAIS (não a escala comprimida 1-7 antiga). Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de Lutar Várias Vezes + trocar VÁRIOS Pokétickets por Energia** (esta sessão): "Começa implementando um recurso de combate de várias vezes nas Áreas de captura... ou dar a opção de ele usar TODA a energia disponível. O carregamento de energia por tickets, colocar uma opção nas áreas que usam energia para carregar múltiplos tickets, caso no máximo 10." Duas mudanças independentes, ambas testadas: **(1) Lutar Várias Vezes** -- `DT.gaIniciarLutasVarias` confirmado abrindo a sequência de verdade e já disparando a 1ª luta sozinha; fechar cada luta (`DT.gaEncerrarBatalha`, o mesmo botão "Continuar"/"Deixar ir embora" de sempre) confirmado encadeando a PRÓXIMA luta automaticamente (sem precisar clicar "Entrar na mata" de novo), decrementando o contador certo a cada uma; a sequência termina sozinha ao completar a quantidade pedida, com aviso final mostrando o placar; `DT.gaIniciarLutasComTodaEnergia` confirmado calculando certo o máximo de lutas que cabem na Energia atual (37 Energia / 5 por luta = 7 lutas), recusando educadamente quando não dá nem pra 1 (leva seguinte reforçou a trava -- ver abaixo). **(2) Trocar VÁRIOS Pokétickets por Energia** -- `DT.gaTrocarTicketPorEnergia` confirmado trocando a quantidade digitada de verdade; travando em 10 por vez mesmo com estoque de sobra; travando no estoque real quando ele é MENOR que 10; nunca deixando a Energia passar de 1000 -- pedindo mais tickets do que cabe usa só os REALMENTE necessários pra encher (nunca desperdiça o resto) e avisa que usou menos do que foi pedido; recusando educadamente sem Energia-no-teto ou sem Pokéticket nenhum; o widget confirmado aparecendo de verdade nas 3 telas que gastam Energia (Início/Área de Captura/Mestres). Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de Terreno em batalha** (esta sessão): "Depois disso pode dar continuidade pro lance do terreno." Mesmo padrão do Clima (estado por luta, 5 turnos, armado pelos Movimentos oficiais), mas os dois convivem juntos sem se cancelar. Confirmado: `noChao()` reconhece Pikachu/Bulbasaur (sem Tipo Voador) como "no chão" e Pidgey (Normal/Voador) como "no ar"; `terrainDamageMultiplier` confirmado ×1,3 pro Tipo certo (Elétrico/Grama/Psíquico) só em quem está no chão, ×0,5 pro golpe de Dragão na Névoa, sem efeito nenhum em quem está no ar; Clima (Chuva) e Terreno (Elétrico) confirmados valendo AO MESMO TEMPO, sem um cancelar o outro; Electric Terrain de verdade arma o Terreno por 5 turnos numa luta real; Thunderbolt (Elétrico) causou mais dano de verdade sob Terreno Elétrico do que sob Neutro; residual do Terreno de Grama confirmado curando quem está no chão mas NÃO quem está no ar, com a contagem de turnos revertendo sozinha pro Neutro; Terreno Elétrico confirmado impedindo Sono só em quem está no chão (quem está no ar ainda pega Sono normalmente); Terreno de Névoa confirmado impedindo Condição persistente pela mesma régua. Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de travar a quantidade de Lutar Várias Vezes na Energia atual** (esta sessão): "A quantidade de lutas tem que ser limitado a energia atual que o jogador possui. Então se ele tem 100 de energia e vai fazer uma luta que custa 5, ele pode lutar no máximo 20. Trava isso no combate desejado usando a lógica certa." `iniciarSequenciaDeCaptura` agora calcula o máximo pagável (`Energia atual ÷ custo da Zona`) ANTES de abrir a sequência, e trava a quantidade PEDIDA nesse máximo -- confirmado com o exemplo exato do pedido (100 de Energia, Zona de custo 5 → pedir 50 lutas trava em 20, com aviso explicando a trava); pedir menos do que cabe usa exatamente o valor digitado, sem aviso nenhum; pedir exatamente o máximo também não dispara aviso de trava (só avisa quando o pedido realmente precisou ser cortado). O campo de quantidade na tela também ganhou `max` e um `title` mostrando o teto real da Zona. Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão.

**Leva de automação total da sequência múltipla + custo uniforme dos Mestres** (esta sessão): "Não, ele tem que dar o aviso ANTES de começar a luta e não deixar seguir, ou o número VOLTAR pra quantidade máximo que o usuário pode. Nunca deixar passar e dar o aviso lá em cima. [...] batalhas múltiplas [...] tem que ser automáticas. Não parando nem pra evolução. [...] o jogo pergunta se ele quer tentar que o sistema capture, cure automaticamente [...] elas precisam ser independentes. Se ele não selecionar nenhuma a batalha segue sem uso de nada que ele pediu. Se ele selecionar o sistema vai tentar capturar e curar sempre usando do item mais recomendado para a situação. A parte da evolução [...] será feita após a sequencia de combate [...]. Os mestres, precisam ter uma entrada uniforme de energia. Todos eles vão custar 300 energias." + interjeição no meio da mesma leva: "Se eu atualizo a tela, no meio do combate multiplo, ele simplesmente perde a sequencia. Isso não pode ocorrer. O sistema tem que rodar por trás independente da minha tela. Se eu atualizar e voltar pra área de captura ele tem que estar lá pra eu ver o que aconteceu." Quatro mudanças amarradas na mesma peça nova, todas testadas:

**(A) Nunca mais clampa-e-segue** -- a leva anterior travava a quantidade pedida e começava a luta mesmo assim, só avisando depois; agora `iniciarSequenciaDeCaptura` recusa CRIAR qualquer luta quando o pedido passa da Energia atual: mostra o aviso no banner do topo (`SESSION.error`, "lá em cima") e devolve o campo pro número máximo (`SESSION.capturaQtdSugerida`, lido de volta no `value` do `<input>`) -- o jogador precisa clicar de novo, já com o número certo, pra confirmar.

**(B) Automação total pra 2+ lutas** -- pedir 1 luta só continua sendo o clique manual de sempre ("isso é só para o caso de multiplos combates"); pedir 2+ abre `SESSION.pendingSequenciaConfirm`, um popup com 2 `<input type=checkbox>` **independentes** (🎯 capturar automaticamente / 💊 curar automaticamente), respondido por `DT.gaConfirmarSequencia`. A partir daí a sequência inteira roda sem pausa nenhuma pro jogador: `resolverLutaAutomaticamente` resolve pendingHeal/pendingSwitch/pendingEvolution/Pokébola sozinho, reaproveitando os MESMOS `DT.ga*` do fluxo manual (`gaUsarItemCura`/`gaRecusarItemCura`/`gaTrocarPokemon`/`gaJogarPokebola`/`gaResponderEvolucao`) -- nunca uma cópia da lógica. Toggle desligado = sempre recusa o gatilho, sem gastar nada; ligado = usa `itemCuraRecomendado`/`pokebolaRecomendada` (o menor item que já cobre o PV que falta, senão o melhor disponível; a bola mais fraca que já bate ~70% de captura, senão a mais forte disponível). Evolução é SEMPRE recusada na hora (nunca decidida no meio -- recusar não é destrutivo, `eligibleEvolution` já garante que ela reaparece sozinha no próximo level up) e a decisão de verdade fica pra depois: ao terminar a sequência inteira, `elegiveisParaEvoluirAgora()` varre a Equipe de novo (sem precisar guardar nenhum histórico de "quem evoluiu durante") e mostra uma tela de resumo com um botão Evoluir/Manter por Pokémon elegível (`DT.gaEvoluirPosSequencia`).

**(C) Custo uniforme dos Mestres** -- `startBattle`/`ginasiosScreenHTML` trocam `20 × Nível do Ace` (que variava Mestre a Mestre) por `MESTRE_CUSTO_ENERGIA = 300`, igual pros 8.

**(D) Sobrevive a um F5 de verdade** -- todo o estado da sequência (que Zona, quantos faltam, placar, os 2 toggles, se já terminou) vive em `STATE.captureSequence`, NUNCA em `SESSION` (que é só memória da aba, zerada a cada carregamento) -- então é salvo no Firestore pela MESMA `mutate()`/`queueSave()` de sempre. Quem empurra a sequência pra frente é `captureSequenceTick()`, chamado (1) na hora, ao confirmar o popup, e (2) sozinho, por um `setInterval` de fundo (mesmo padrão do contador de Energia) que roda INDEPENDENTE de qual tela está aberta -- resolvendo 1 luta inteira por tick, nunca a sequência toda de uma vez. Confirmado batendo um "F5" de verdade no teste (zera `STATE`, reconecta pelo MESMO `attachSaveListener` que uma sessão salva usa) no meio de uma sequência: o progresso exato (total/vitórias/toggles) volta do Firestore, e o tick de fundo continua sozinho até terminar. Limite documentado e honesto: só sobrevive enquanto a ABA continua aberta -- fechar a aba de verdade pausa o relógio (não existe backend/Cloud Function aqui, só o Firestore como banco); reabrir retoma normal. Enquanto uma sequência está ativa, `DT.gaEntrarCaptura`/`DT.gaDesafiarMestre` recusam abrir qualquer luta manual nova por fora dela (evita disputar a mesma `STATE.battle` com o tick).

Suíte completa (`test.js`/`test_firebase.js`/`stress.js`/`test_dom_globals.js`) sem regressão, incluindo o cenário de F5 simulado. Fora dessa leva: as setinhas nativas de incrementar/decrementar de TODO `<input type="number">` do jogo foram removidas via CSS ("Tire as setinhas de colocar os números para que a gente só digite, fica mais bonito") -- só digita, sem clicar em seta nenhuma.

**Leva do relatório final da sequência automática** (esta sessão): "Na sequencia de luta, gere um relatório final de cada luta como foi cada uma e o resultado, se teve captura, uso de poção etc." Cada luta fechada DENTRO de uma sequência automática (`fecharLutaDaSequencia`) agora empilha 1 entrada em `STATE.captureSequence.relatorio` (via `battleReportEntry`) -- persistida junto com o resto da sequência, então sobrevive a um F5 igualzinho. Cada entrada guarda: número da luta, tipo de oponente (selvagem/Treinador/Treinador de Elite/Mestre) e o rótulo dela, resultado (vitória/derrota), Pokédólares e Pokétickets ganhos (lidos de `b.premioGanho`/`b.ticketsGanhos`, guardados na hora por `finishBattle` -- NUNCA recalculados depois, porque `ticketReward()` sorteia de novo pro Treinador de Elite e daria um número diferente do que realmente foi creditado), quais itens de cura foram usados de verdade (`b.itensUsadosSequencia`, empilhado direto em `resolverLutaAutomaticamente` no momento da decisão), se tentou capturar e o desfecho -- sucesso ou fuga -- com a bola e a Espécie (`b.capturaTentativaSequencia`), e se precisou trocar de Pokémon por desmaio. A tela de resumo no fim da sequência (`sequenciaRelatorioHTML`) mostra 1 linha-resumo por luta, cada uma um `<details>` nativo (sem JavaScript nenhum pra abrir/fechar) que revela o log turno-a-turno completo daquela luta específica quando clicada. Confirmado: as 3 lutas de uma sequência de teste geraram 3 entradas na ordem certa, cada uma com resultado/prêmio/log reais; e uma luta sintética com cura E captura ligadas ao mesmo tempo registrou os dois eventos na MESMA entrada. Suíte completa sem regressão.

**Leva de revisão da Wiki** (esta sessão): "Preciso que revise a wiki e veja se não tá errado nada ali. Pois a informação de XP está incorreta." A tela 📖 Wiki (`wikiScreenHTML`) carregava texto de LEVAS ANTIGAS que nunca tinha sido atualizado quando as mecânicas reais mudaram -- nenhuma suíte de teste sequer renderizava essa tela até agora, então nada pegava a divergência. Três erros de verdade corrigidos, todos conferidos direto contra as funções reais do jogo:
- **XP** -- a Wiki dizia "vencer na sua Faixa, na anterior ou na seguinte dá 1 XP" e "2+ Faixas acima dá XP igual à diferença". A regra REAL (`xpParaVitoria`) é outra: mesma Faixa = **2 XP** (base), 1 Faixa abaixo = 1 XP, 2+ abaixo = 0, e cada Faixa ACIMA soma **+1 XP aos 2 de base** (1 acima = 3, 2 acima = 4, 3 acima = 5...) -- a Wiki agora descreve essa tabela certa.
- **Custo de Energia** -- a Wiki ainda descrevia o sistema ANTIGO por Nível do oponente ("selvagem = 10× o Nível dele... Mestre = 20× o Nível do Ace"), de 2 levas atrás. O custo real hoje é FIXO por Zona (`custoEnergiaZona`, 5/10/15.../50) pra selvagem/Treinador/Treinador de Elite, e FIXO em 300 (`MESTRE_CUSTO_ENERGIA`) pra qualquer Mestre -- a Wiki e o tooltip de Energia (🔋 no topo, mesmo texto reaproveitado) foram reescritos com os números certos.
- **Chance de Treinador por Zona** -- a Wiki apontava "87% na Elite"; o pico real de 87% (`chanceTreinadorZona`) acontece na Zona LENDÁRIA (a última, índice 9), não na Elite (índice 8, que fica em 79%) -- corrigido, com a progressão completa Zona a Zona.

Também documentada na Wiki, pela primeira vez, a automação de "Lutar Várias Vezes" (toggles de captura/cura automática, evolução adiada, persistência a F5) -- que já existia no jogo mas não tinha explicação nenhuma lá. Suíte completa sem regressão, e um teste NOVO garante que a Wiki renderizada de verdade bate com `xpParaVitoria`/`custoEnergiaZona`/`MESTRE_CUSTO_ENERGIA` (as funções reais) e que nenhum resquício do texto antigo/errado sobrou.

**Leva do Centro Pokémon -- Fase 1 de 4 (Diretório + Chat público + Amizade)** (esta sessão): "Quero criar uma área, chamada Centro Pokémon. Ali os jogadores vão poder trocar pokémons, itens, dinheiro. Além de trocar chat e se desafiarem para um pvp (funciona igual a luta na mata, mas contra outro player). O chat é livre para todos verem o que todos falam. Crie também um chat privado (que pode ser acessado de qualquer área) além de pedido de amizade entre os jogadores." Pedido grande o bastante (Centro Pokémon + troca ao vivo + PvP sincronizado + chat público + chat privado + amizade) pra dividir em fases -- confirmado com o usuário: fasear em vez de tentar tudo de uma vez, troca ao vivo (não caixa de correio) e PvP como duelo sincronizado de verdade (não resolução instantânea contra uma "foto" do time do outro). Esta é a **Fase 1**: diretório de Treinadores, chat público e sistema de amizade -- troca ao vivo, chat privado e PvP ficam pras Fases 2-4.

**Arquitetura nova, decisão de propósito**: até aqui o jogo era single-player de verdade -- cada conta só sincronizava o PRÓPRIO save (`onSnapshot` no próprio documento). Multiplayer de verdade exige escrever no documento de OUTRO jogador (mandar mensagem, pedido de amizade), e isso tem um risco real de corrida: `doSave()`/`mutate()` sempre reescrevem o campo `state` INTEIRO da própria conta de uma vez, então qualquer coisa que outro jogador tivesse escrito ali dentro seria apagada sem querer na próxima vez que o dono salvasse qualquer coisa, mesmo sem relação nenhuma com o que chegou. Resolvido guardando tudo que é "social" em **3 coleções novas, no TOPO do banco** (nunca dentro do `state` de `green_area_saves`) -- `green_area_chat_publico` (1 documento, mensagens do chat), `green_area_inbox` (1 documento por conta, pedidos de amizade RECEBIDOS) e `green_area_amizades` (1 documento por PAR confirmado, id = os 2 docIds ordenados unidos por `__`) -- e escrevendo nelas SEMPRE via `firebase.firestore.FieldValue.arrayUnion`/`arrayRemove` (operação atômica de verdade do lado do servidor, nunca "lê o array inteiro, edita, escreve de volta") -- 2 jogadores mexendo ao mesmo tempo nunca se atropelam. O inbox e a lista de amizades escutam **desde o login** (junto do save normal, `attachSocialListeners`, mesmo padrão "roda em segundo plano independente da tela" já usado pro contador de Energia e pela sequência automática de lutas) -- um pedido de amizade chega e aparece (com um selo vermelho no menu) mesmo se o jogador nunca tiver entrado no Centro Pokémon. Já o chat público só liga o listener ENQUANTO o jogador está olhando aquela tela especificamente (`DT.gaAbrirCentro`/`DT.gaGoto`), economizando banda.

**Testado** com **2 e 3 contas de verdade interagindo na mesma suíte** (`test_social.js`, arquivo NOVO -- nenhuma outra suíte testava várias contas conversando entre si): diretório nunca lista a própria conta; mensagem mandada por uma conta aparece de verdade pra outra conta que nunca esteve online ao mesmo tempo (histórico persiste no Firestore, não é "ao vivo ou nada"); mensagem em branco não manda nada; mensagem gigante é cortada no teto (`CENTRO_CHAT_MSG_MAX_LEN`, 300 caracteres); histórico trava em `CENTRO_CHAT_MAX` (200) mensagens, cortando as mais ANTIGAS primeiro (testado semeando 205 mensagens direto no banco e confirmando que 1 mensagem nova de verdade já aciona o corte, sem nunca cortar a mais nova); sair da tela do Centro Pokémon desliga o listener do chat ao vivo. Pedido de amizade confirmado chegando no inbox do destinatário mesmo sem ele estar online no momento do envio; mandar 2 pedidos pro MESMO Treinador é recusado (sem duplicar); aceitar cria a amizade confirmada e os DOIS lados descobrem sozinhos (sem round-trip nenhum de volta pro remetente -- só reler a coleção `green_area_amizades`); recusar nunca cria amizade nenhuma; mandar pedido pra quem já é amigo é recusado; desfazer amizade remove o par e os 2 lados descobrem ao vivo. Também: `firestore_stub.js` extraído da cópia colada em CADA suíte de teste (eram 4 cópias idênticas) pra um módulo compartilhado só, com um bug real corrigido na extração -- a versão antiga de `collection()` sempre devolvia o MESMO Map de documentos, não importa o nome pedido (nunca dava problema porque só existia 1 coleção no jogo inteiro; passaria a colidir de verdade assim que uma 2ª coleção fosse aberta) -- agora cada nome de coleção tem seu próprio armazenamento isolado, e o stub ganhou `onSnapshot` na COLEÇÃO inteira (não só documento) + `FieldValue.arrayUnion`/`arrayRemove`/`serverTimestamp` de verdade. `test_dom_globals.js` ganhou um clique real na tela nova (abrir o Centro Pokémon, mandar um pedido de amizade de verdade) confirmando que os `onclick="..."` novos (Adicionar amigo/Aceitar/Recusar/Desfazer/chat) só usam identificadores globais de verdade -- mesma varredura que já pegou o bug de botão morto antes. Suíte completa (as 5 agora) sem regressão.

**Correção -- Diretório por presença ao vivo + diagnóstico real de erro no Centro Pokémon** (esta sessão): dois pedidos. (1) "No centro pokémon só quero que apareçam os treinadores que estiverem no menu do Centro pokémon, ao sair ele sai dali também" -- o diretório da Fase 1 listava TODA conta existente (`savesCol.get()`, igual ao Painel Admin); trocado por **presença ao vivo de verdade**: `green_area_centro_presenca` (coleção nova, 1 documento por conta, `{nome, ultimoPing}`) recebe um "ping" ao abrir a tela e se renova sozinho a cada 25s (`CENTRO_PRESENCA_HEARTBEAT_MS`) enquanto ela continuar aberta; sair da tela (`DT.gaGoto` pra outra tela), sair da conta (`DT.gaSair`) ou fechar a aba/navegador (`beforeunload`) apaga o próprio ping na hora -- e, como não existe backend nenhum pra limpar um ping órfão de uma aba que travou ou foi fechada sem disparar `beforeunload`, todo ping mais velho que 75s (`CENTRO_PRESENCA_TIMEOUT_MS`, 3× o intervalo do heartbeat) é filtrado no cliente como "fantasma" -- limitação documentada, mesmo padrão já usado noutro lugar do projeto (a sequência automática de captura também só sobrevive de verdade enquanto a aba segue aberta). Como cada conta só escreve o PRÓPRIO documento de presença (nunca o de outro jogador), essa é a única escrita social do Centro Pokémon que **não** precisa de `FieldValue.arrayUnion` -- um `set()` direto já é seguro contra corrida. (2) "Erro encontrado ao enviar pedido de amizade, mensagem. resolva" -- toda mensagem de erro social (mandar pedido/mensagem, e os listeners de fundo do inbox/lista de amigos que antes falhavam em silêncio) trocou o "verifique sua internet" genérico pelo **motivo real devolvido pelo Firestore** (`erroFirestoreMsg`, lê `err.message`). A causa mais provável pro erro relatado: as regras de segurança do Firestore do projeto do usuário ainda não tinham sido atualizadas pra autorizar as coleções novas do Centro Pokémon (passo manual, documentado em "Habilitar o salvamento online" acima, que só quem tem acesso ao Firebase Console consegue aplicar) -- a mensagem de erro nova deixa isso visível na hora em vez de esconder atrás de um texto genérico. **Testado**: `test_social.js` e `test_dom_globals.js` reescritos pra simular contas de verdade "ao vivo" (escrevendo o próprio ping de presença direto no banco, igual o heartbeat faria) nos pontos em que o teste precisa que outra conta apareça no diretório -- incluindo um caso de ping velho demais sendo filtrado como fantasma. Suíte completa (as 5) sem regressão.

**Leva do Centro Pokémon -- Fase 2 de 4 (Chat privado)** (esta sessão): "Após isso, siga para a fase 2" -- Fase 2 do pedido original ("Crie também um chat privado que pode ser acessado de qualquer área"). Nova tela **💬 Mensagens** (item próprio no menu lateral, visível de qualquer tela, com selo de não-lidas -- mesmo padrão do selo de pedido de amizade do Centro Pokémon). MESMA arquitetura de dado do chat público (documento por conversa, mensagens sempre via `FieldValue.arrayUnion`), só que **1 documento por PAR de Treinadores** (`green_area_dm/{par}`, reaproveitando `friendPairId` -- mesmo id de par que `green_area_amizades` usa, mas numa coleção separada, então os 2 documentos nunca colidem) em vez de 1 documento global. Só dá pra conversar com quem já é **amigo** (o sistema de amizade da Fase 1 já é o "aceite" de verdade). "Acessível de qualquer área": ao contrário do chat público (que só liga o listener dentro da tela do Centro Pokémon), o listener de `green_area_dm` roda **desde o login** (`attachSocialListeners`, mesmo padrão do inbox de amizade) -- o selo de mensagens novas já acende de qualquer tela, e só o HISTÓRICO de uma conversa específica é buscado ao abrir a tela de Mensagens. Controle de leitura ("lido até quando") fica no PRÓPRIO save de cada Treinador (`STATE.dmLidoAte`), nunca no documento compartilhado -- sem risco de atropelo nenhum, já que só o dono mexe no próprio campo. Abrir uma conversa marca como lida na hora; uma mensagem nova chegando com a conversa JÁ aberta também marca como lida na hora (nunca fica um selo aceso numa conversa que o jogador está olhando). Mesmas travas do chat público: histórico por conversa trava em `DM_CHAT_MAX` (200) mensagens com trim automático, mensagem individual cortada em `DM_MSG_MAX_LEN` (300) caracteres.

**Testado**: nova seção em `test_social.js` (2 contas amigas -- Fabio e Eva) cobrindo: mandar mensagem privada pra quem NÃO é amigo é recusado; mensagem em branco não manda nada; mensagem chega de verdade pro outro lado MESMO sem ele estar na tela de Mensagens (roda em segundo plano desde o login, igual o inbox de amizade); contagem de não-lidas certinha (por conversa E o total somado, usado no selo do menu); abrir a conversa marca como lida na hora; a PRÓPRIA mensagem nunca conta como não lida pra quem mandou; trava de tamanho de mensagem e de histórico (mesmo padrão do chat público, testado semeando um histórico gigante direto no banco). `test_dom_globals.js` ganhou um clique real na tela nova (abrir 💬 Mensagens, abrir uma conversa, mandar uma mensagem privada de verdade, voltar) confirmando que os `onclick="..."` novos (`gaGoto('mensagens')`/`gaAbrirConversa`/`gaEnviarMensagemPrivada`/`gaFecharConversa`) só usam identificadores globais de verdade. Suíte completa (as 5) sem regressão.

**Filtro/busca na Caixa** (esta sessão): "Cria uma área de filtro na caixa dos pokémon pra facilitar encontrar os pokemon que precisamos, um filtro parecido com a pokedex" -- reaproveita 100% o padrão de filtro já usado na 📕 Pokédex (`dexFilteredList`/`dexScreenHTML`) e no 🗡️ Catálogo de Movimentos (`movDexFilteredList`/`movesScreenHTML`): busca por apelido/nome/número (`caixaFilteredList`), Tipo 1/Tipo 2 combináveis e Papel de Combate (mesmas 4 categorias já usadas na Equipe, `ROLES_COMBATE`/`PAPEL_COMBATE_LABEL`) -- um Pokémon da Caixa sem Papel setado ainda conta como "⚔️ Ataque" (mesmo default usado na Equipe). Só a Caixa ganhou filtro -- a Equipe é sempre ≤6, não precisa. **Testado**: nova seção em `test.js` com uma Caixa de composição CONHECIDA (Charmander/Charizard/Bulbasaur, incluindo 1 sem Papel de Combate setado de propósito) cobrindo busca por apelido/nome/número, Tipo 1, Tipo 2, Papel de Combate (inclusive o default "Ataque" pra quem nunca teve Papel setado) e filtros combinados entre si -- suíte completa sem regressão.

**Leva do Centro Pokémon -- Fase 3 de 4 (Troca ao vivo)** (esta sessão): "retome o filtro da caixa e depois siga para fase 3" -- Fase 3 do pedido original ("trocar pokémons, itens, dinheiro"), confirmada com o usuário desde o início como **troca ao vivo** (os 2 online montando a oferta em tempo real, não uma caixa de correio assíncrona). Nova coleção `green_area_trocas` -- **1 documento por troca** (id aleatório, `uid()`, nunca um par determinístico como `friendPairId`: o MESMO par de amigos pode trocar várias vezes ao longo do tempo, uma troca concluída vira um documento apagado e a próxima é outro documento do zero). Só entre **amigos** (botão "🔄 Trocar"/"🔄 Continuar troca" na lista de amigos do Centro Pokémon, que reaproveita uma troca já ativa em vez de criar duplicata). Cada lado escreve SÓ o PRÓPRIO campo do documento (`ofertaA`/`confirmadoA`/`recebidoA` só por quem é o lado "a", idem "b") -- 2 jogadores editando a mesma troca ao mesmo tempo nunca se atropelam porque nunca escrevem o MESMO campo (mesmo raciocínio da presença do Centro Pokémon: campo exclusivo do dono, `.update()` comum já basta, sem precisar de `arrayUnion`). Editar a própria oferta zera as 2 confirmações de novo (mesmo a do outro lado) -- garante que ninguém confirma um negócio que já mudou; pior cenário de uma corrida nesse reset é só precisar clicar em Confirmar de novo, NUNCA troca a mais ou a menos, porque a execução só dispara quando os 2 `confirmado*` aparecem `true` no MESMO snapshot. Oferta = Pokédólares + Pokébolas/Poções + Pokémon **só da Caixa** (nunca da Equipe, decisão de propósito -- evita o caso de esvaziar o time de alguém no meio de uma troca; cada Pokémon oferecido carrega uma CÓPIA COMPLETA do objeto no documento compartilhado, não só o id, porque é assim que o outro lado consegue "receber" um Pokémon que nunca existiu no próprio save). **Execução 100% client-side, sem transação de servidor**: quando os 2 lados confirmam, CADA CLIENTE aplica a PRÓPRIA metade sozinho (tira o que ofereceu, dá o que recebeu, salva no PRÓPRIO `green_area_saves` via `mutate()` normal) e marca o PRÓPRIO `recebido*` -- nenhum lado NUNCA escreve no save do outro (mesma arquitetura de segurança do resto do Centro Pokémon). Quando os 2 `recebido*` ficam `true`, o documento da troca é apagado sozinho (limpeza). Isso roda em segundo plano desde o LOGIN (`attachSocialListeners`, mesmo padrão do inbox/DM) -- a troca sempre conclui pros 2 lados mesmo que um deles já tenha saído da tela de Troca depois de confirmar. Cancelar só é possível **antes** de qualquer lado ter aplicado a própria metade (`!recebidoA && !recebidoB`) -- depois disso, alguma coisa já mudou de mãos, não dá mais pra desfazer.

**Limitação conhecida, documentada de propósito**: a oferta é validada contra o estoque ATUAL antes de deixar confirmar, e de novo (defensivamente) antes de executar -- mas se um jogador gastar os recursos oferecidos DEPOIS de confirmar a troca e ANTES dos 2 lados confirmarem (uma corrida bem específica entre 2 abas), o outro lado ainda recebe o combinado (a execução usa a CÓPIA snapshotada no momento da oferta, não uma releitura ao vivo do estoque do remetente -- ler o estoque de OUTRO jogador reintroduziria exatamente o risco de atropelo que essa arquitetura inteira foi desenhada pra evitar). Numa troca entre amigos de mesa o risco é desprezível, mas fica registrado aqui, no mesmo espírito dos outros avisos "sem garantia 100%" já espalhados pelo projeto.

**Testado**: nova seção em `test_social.js` (2 contas amigas -- Gara e Hugo, cada uma com 1 Pokémon na Caixa e Pokédólares/Pokébolas conhecidos) cobrindo: iniciar troca com quem NÃO é amigo é recusado; montar a oferta (dinheiro/itens/Pokémon) atualiza o documento de verdade e chega ao vivo pro outro lado mesmo sem ele estar na tela de Troca; oferecer mais dinheiro do que se tem trava no valor real; confirmar com uma oferta que não cabe mais no estoque atual é recusado; os 2 lados confirmando dispara a execução automática, testada de ponta a ponta (Pokédólares/Pokébolas/Pokémon chegam certinho pros 2 lados, a Caixa de quem ofereceu perde o Pokémon, o documento da troca some sozinho no fim); reabrir `gaIniciarTroca` com uma troca já ativa reaproveita a MESMA (nunca duplica); cancelar antes de qualquer confirmação apaga a troca de verdade; cancelar depois que um lado já recebeu a própria metade é recusado. `test_dom_globals.js` ganhou um clique real na tela nova (abrir 🔄 Trocar, definir dinheiro, ajustar Pokébola, confirmar, cancelar) confirmando que os `onclick="..."` novos só usam identificadores globais de verdade. Suíte completa (as 5) sem regressão.

**Pokétickets iniciais** (esta sessão): "Novos jogadores começam com 100 tickets" -- `freshState()` agora começa com `poketickets: 100` em vez de 0 (contas já existentes não mudam -- só afeta conta CRIADA a partir de agora). **Testado**: nova assert na seção de criação de conta em `test.js`; suíte completa sem regressão.

**Popup de evolução como prioridade** (esta sessão): "Quando o pokémon evoluir faça com que um popup surja na tela como prioridade e assim o jogador decide se vai evoluir ou não" -- a pergunta de evolução no meio de uma luta manual (`DT.gaResponderEvolucao`) era só um card `.ga-battle-prompt` dentro da própria tela de batalha (mesma "caixa" do gatilho de cura e da troca por desmaio); agora usa `.ga-overlay`/`.ga-overlay-card` -- o MESMO componente de popup de tela cheia (z-index acima de tudo) já usado pelos outros popups de prioridade do jogo (Energia insuficiente, Luta muito difícil, Sequência automática), com o sprite da forma evoluída. Clicar fora do card ou no ✕ conta como "Não evoluir" -- mesmo padrão dos outros popups (fechar = recusar). A evolução ADIADA de uma sequência automática (fora de luta, ver "Área de Captura" acima) já era a tela inteira desde a leva que a criou, não precisou de mudança. **Testado**: nova assert em `test.js` confirmando que a tela renderizada de verdade com uma evolução pendente contém o popup (`ga-overlay`) em vez do card antigo; suíte completa sem regressão.

## Em aberto (próximos passos possíveis)

- **Centro Pokémon, Fase 4 -- PvP** como duelo sincronizado de verdade entre 2 jogadores (cada um decidindo cura/troca/Pokébola do próprio lado, ao vivo -- não uma resolução instantânea contra uma "foto" parada do time do outro).
- Clima/Terreno condicionando Habilidade (Leaf Guard/Ice Body/Rain Dish etc.), Weather Ball mudando de Tipo/poder, Solar Beam sem carregar no Sol, e o bloqueio de golpe com Prioridade sob Terreno Psíquico (Green Área ainda não simula Habilidade em batalha nem tem golpe de carregar 2 turnos, e bloquear Prioridade exige interceptar a escolha de ação antes da fórmula de dano -- mecanismo bespoke, fora de escopo por ora).
- Levitate/item segurado (Air Balloon etc.) isentando um Pokémon do efeito de Terreno mesmo sem ser Tipo Voador -- depende de Habilidade/Item reais em batalha, que ainda não existem aqui.
- **Item segurado em batalha** com efeito original (hoje Mochila/itens só existem fora de luta, pra cura/Pokébola/MT).
- **Habilidade com gatilho real em combate** (Intimidate, Levitate etc. — hoje é só identidade exibida; o RPG principal já tem um motor de gatilhos de Habilidade, `ABILITY_MECHANICS`, que dá pra reaproveitar).
- **Mecânicas específicas ainda não cobertas pelo motor genérico**: Substituto, Semente Maligna (Leech Seed), armadilhas de campo (Stealth Rock/Toxic Spikes já existem no papel de Movimento 'controle', mas sem o efeito de armadilha de verdade), golpes que preparam o clima, golpes de carregar 2 turnos (Solar Beam etc.), Nocaute em 1 Golpe, golpes de dano fixo, Counter/Mirror Coat, **Movimentos de troca forçada** (U-turn/Volt Switch/Baton Pass — ver tarefa própria).
- Item que permita trocar a Habilidade ou a Natureza de um Pokémon já capturado (hoje as duas vêm aleatórias na captura/recebimento e ficam fixas para sempre, por pedido explícito).
- Modo "visitar" o progresso de outro Treinador só de leitura (a base de dados já dá pra isso — hoje só falta a tela).
- Log de batalha com playback (hoje é tudo calculado e mostrado de uma vez até a próxima pausa; dava pra revelar linha por linha com uma pequena animação).
- Elenco curado de Treinadores aleatórios de Zona (hoje é genérico: nome de uma lista + Pokémon aleatórios de uma piscina mais ampla) — os 8 Mestres já são curados à mão, isso é só pros Treinadores que aparecem andando na Zona.
- Mais itens na Loja além dos de cura/Pokébola/MT (Bulbapedia tem o catálogo oficial inteiro pra puxar de lá quando fizer sentido, mesma fonte que orientou os preços/multiplicadores relativos).
