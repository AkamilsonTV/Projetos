# Pokémon RPG — Diário de Treinador

- **Regras** (livro consolidado, v0.2): [`regras.md`](regras.md)
- **Código-fonte do site**: [`site/diario-de-treinador.html`](site/diario-de-treinador.html)
- **Site publicado**: veja a seção [Publicar no GitHub Pages](#publicar-no-github-pages-uma-vez) abaixo — depois de ativado, o link fica em `https://<seu-usuário>.github.io/<repositório>/`.

O site é uma página HTML única, hospedada de graça no **GitHub Pages**, com os dados (fichas de todo mundo) salvos em tempo real num banco **Firebase Firestore** (também grátis, sem cartão de crédito). Sem esse banco configurado, o site abre mas mostra uma tela avisando que falta ligar o banco de dados.

## Visual e sprites

O tema é mais "jogo" que "wiki": fundo em tom pergaminho, cantos arredondados, títulos numa fonte pixelada (Press Start 2P) e badges de Tipo em formato de pílula. Cada Pokémon na ficha mostra automaticamente o sprite pixelado oficial da espécie (via [PokeAPI/sprites](https://github.com/PokeAPI/sprites), puxado pelo número da Pokédex salvo quando a Espécie é preenchida) — sem espécie definida, mostra um "❔" no lugar.

## Regras e guias dentro do site

O botão **"📖 Regras"** (no topo, em qualquer tela) abre três sub-abas, cada uma buscando o `.md` correspondente direto deste repositório — não são cópias coladas no site:

- **📜 Regras** — [`regras.md`](regras.md), o livro de regras.
- **🎒 Guia do Treinador** — [`guia-treinador.md`](guia-treinador.md), passo a passo de uso pra quem joga: desde entrar/criar personagem até cada função da própria ficha (tema claro/escuro incluso).
- **🛠️ Guia do Mestre** — [`guia-mestre.md`](guia-mestre.md), o mesmo tipo de passo a passo, mas das funções exclusivas do Mestre.

Como nenhum desses é uma cópia colada no HTML: sempre que a gente atualizar qualquer um dos três arquivos e enviar o commit, a próxima vez que alguém abrir aquela aba no site já vê a versão nova, sem precisar tocar no código. **Toda função nova do site precisa ganhar uma explicação nesses guias** — ao contrário do livro de regras (que só existe pra descrever o sistema em si), os guias existem pra ensinar a usar o que já está em vigor no site, então não podem ficar desatualizados. Funciona antes mesmo de configurar o Firebase.

## Como usar o site

- **Mestre**: define um PIN na primeira vez que entrar em "Sou o Mestre"; depois disso, usa esse mesmo PIN pra sempre. Vê e edita a ficha de todos os Treinadores da mesa, incluindo Nível e Vínculo (que os jogadores só enxergam, não editam) e o contador de batalhas vencidas (visível só pro Mestre).
- **Treinador**: cria a própria ficha (nome + PIN) em "Sou Treinador → Criar personagem"; depois entra de novo com nome + PIN. Pode ativar "Compartilhar com a mesa" pra deixar outros Treinadores verem a própria ficha (somente leitura).

### Progressão de Nível

A cada 4 batalhas vencidas (contador que só o Mestre vê e mexe), o Treinador sobe 1 Nível automaticamente e o contador zera. Ver [`regras.md`](regras.md#24-progressão-de-nível) para detalhes.

## Configurar o banco de dados (Firebase) — uma vez só

1. Acesse [console.firebase.google.com](https://console.firebase.google.com/) e entre com uma conta Google.
2. **Criar projeto** → dê um nome qualquer (ex: `diario-de-treinador`) → pode desativar o Google Analytics → **Criar projeto**.
3. No menu lateral, vá em **Compilação → Firestore Database** → **Criar banco de dados** → escolha um local (qualquer um serve) → inicie em **modo de teste** (ajustamos as regras de acesso no passo 6).
4. Ainda no console, clique no ícone de engrenagem (⚙️) → **Configurações do projeto**. Na aba **Geral**, role até "Seus apps" → clique no ícone **`</>`** (Web) → dê um apelido ao app → **Registrar app**.
5. O Firebase vai mostrar um bloco `firebaseConfig` parecido com este:
   ```js
   const firebaseConfig = {
     apiKey: "AIzaSy...",
     authDomain: "diario-de-treinador.firebaseapp.com",
     projectId: "diario-de-treinador",
     storageBucket: "diario-de-treinador.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abcdef"
   };
   ```
   Copie esses 6 valores e cole-os no arquivo [`site/diario-de-treinador.html`](site/diario-de-treinador.html), substituindo o bloco `FIREBASE_CONFIG` no topo do `<script>` (procure por `COLE_AQUI`). Depois salve, faça commit e push.
6. **Regras de segurança do Firestore** — no console, vá em **Firestore Database → Regras** e cole exatamente isto (restringe leitura/escrita a só esse documento do jogo, e bloqueia qualquer outra coisa no banco):
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /diario/estado {
         allow read, write: if true;
       }
       match /{document=**} {
         allow read, write: if false;
       }
     }
   }
   ```
   Clique em **Publicar**.

Pronto — depois disso, qualquer pessoa que abrir o site (em qualquer aparelho) lê e escreve no mesmo banco em tempo real: o que um jogador edita aparece pros outros sem precisar recarregar a página.

## Publicar no GitHub Pages (uma vez)

1. No GitHub, abra o repositório → **Settings → Pages**.
2. Em **Source**, escolha **Deploy from a branch**.
3. Em **Branch**, selecione `claude/pokemon-rpg-rules-5bdoj9` e a pasta **`/docs`** → **Save**.
4. Espere alguns minutos; o GitHub mostra o link do site no topo dessa mesma página (algo como `https://<seu-usuário>.github.io/<repositório>/`). O Diário de Treinador fica em `.../pokemon-rpg/site/diario-de-treinador.html` dentro desse link.

## Instalar como app (PC, Mac, Linux, Android)

O site já é um **PWA** (Progressive Web App) — dá pra "instalar" ele como se fosse um programa de verdade, com janela própria, ícone na área de trabalho/barra de tarefas e sem a barra de endereço do navegador, sem precisar baixar nem instalar nada separado:

1. Abra o link do Diário de Treinador (o de `.../pokemon-rpg/site/diario-de-treinador.html`) no **Chrome** ou **Edge**.
2. Clique no ícone de instalação que aparece do lado direito da barra de endereço (um monitor com uma setinha) — ou no menu **⋮ → Salvar e compartilhar → Instalar app "Diário de Treinador"**.
3. Pronto — abre uma janela própria, com a Pokébola como ícone, e um atalho fica salvo (Menu Iniciar/Launchpad/gaveta de apps, dependendo do sistema).

**Ele sempre mostra a versão mais nova do site — não é uma cópia.** A "instalação" só cria um atalho que abre a mesma página ao vivo, então qualquer atualização publicada aqui (Movimento corrigido, regra nova, o que for) já aparece sozinha na próxima vez que abrir o app — e os dados do jogo continuam sincronizando em tempo real pelo Firebase do jeito de sempre, exatamente como no navegador comum. Não tem passo de "atualizar o app": não existe uma versão separada pra atualizar.

Tecnicamente, isso é um `manifest.webmanifest` (nome, ícone, cor do tema) + um Service Worker mínimo (`sw.js`, só existe pra habilitar o botão "Instalar" — não guarda cache de nada, então nunca serve uma versão velha) — os dois arquivos moram do lado do `diario-de-treinador.html`, dentro de `pokemon-rpg/site/`.

## Sincronização entre aparelhos

Sim: os dados do jogo (fichas, PINs, Vínculo, Nível, batalhas etc.) ficam salvos no Firebase, não no navegador — então editar no celular aparece no PC e vice-versa, em tempo real. Só duas coisas ficam presas a cada aparelho, por conveniência, e nunca afetam os dados do jogo: se você está logado (pra não pedir PIN toda hora) e quais fichas estão abertas/fechadas na tela.

## Segurança

Os PINs ficam guardados em texto simples no banco de dados. É uma trava social para o grupo, não um cofre: alguém com acesso técnico ao banco (ou ao link, se as regras de segurança não estiverem configuradas como acima) consegue ver os dados de todo mundo. Não reutilize senhas importantes como PIN aqui.

## Quem edita o quê

- **Mestre**: quem "coloca o Pokémon na ficha" — Espécie, Tipo 1/2, Atributos Naturais e os 4 Movimentos de qualquer Treinador da mesa, além de adicionar/remover Pokémon do time de alguém. Também é o único que edita Estágios e PP (Atual/Máximo) diretamente.
- **Treinador**: cuida do dia a dia da própria ficha — Apelido, PV atual, Condição/Confuso, e **reorganizar os próprios Pokémon** entre Equipe e Banco (ver abaixo). Estágios e PP mudam sozinhos pelo sistema (Movimento usado, item, saída de campo), sem edição direta do jogador. Não escolhe qual Pokémon entra na ficha — isso é só do Mestre.

## Pokédex completa (Gen 1–9) e banco de Movimentos

O site já vem com **todos os 1025 Pokémon** (espécie base de cada um, sem formas regionais/Mega/Gigantamax) e **847 Movimentos oficiais** embutidos — tipos, categoria e Base Stats/Poder/Precisão/Prioridade/**PP** reais, convertidos pelas tabelas das regras (Cap. 6.1 para Atributos, Cap. 9 para Poder de Movimento, Cap. 8.6 para PP). Fonte dos dados: [PokeAPI](https://pokeapi.co/).

- **Espécie** (só o Mestre vê o campo): digite o nome em inglês, como no jogo — assim que bater com um da base, Tipo 1/2 e os seis Atributos Naturais são preenchidos sozinhos, sem precisar clicar em nada.
- **Movimentos, Mestre**: campo "nome" livre com autocomplete, sem restrição (pode digitar qualquer um dos 847) — preenche Tipo/Categoria/Poder/Precisão/Prioridade/Efeito sozinho e continua editável na mão. Movimentos de poder fixo/variável (Seismic Toss, Dragon Rage, Counter, etc.) ficam com Poder em branco — é regra em aberto (Cap. 25), o Mestre adjudica na hora.
- **Movimentos, Treinador**: escolhe os próprios 4 Movimentos, mas restrito ao que a espécie realmente pode aprender — uma barra "Learnset" horizontal por slot, acima da mini-tabela dele, com quatro opções (**Nível** = aprendizado natural, só até o teto liberado pelo Nível do Treinador (Cap. 8.2/8.4); **Evo** = ganho ao evoluir pra essa forma; **TM** = só o que a espécie aprende por Fita, sem depender de Nível; **Egg** = só o que a espécie aprende por Breeding). Os dados de aprendizado (learnset) também vêm do PokeAPI. Não existe opção de HM — a mecânica saiu dos jogos oficiais a partir da Geração 9.
- Z-Moves e Movimentos Dynamax/Gigantamax **não** estão na base — são mecânicas de spin-off sem regra equivalente neste sistema.

## Equipe e Banco (arrastar Pokémon)

Cada Treinador tem 6 posições de **Equipe** (os Pokémon ativos) e um **Banco** sem limite (menu deslizante — botão "🗃️ Banco (N)" abre/fecha). O próprio Treinador (ou o Mestre) reorganiza arrastando pela alcinha **⠿** no canto do card — só isso: quem decide qual Pokémon existe e o que ele é continua sendo o Mestre.

- Arraste um Pokémon do Banco pra um slot vazio da Equipe → ele entra no time.
- Arraste um Pokémon do Banco pra cima de um Pokémon já ativo → eles trocam de lugar (o ativo vai pro Banco).
- Arraste dentro da própria Equipe (ou dentro do próprio Banco) → troca as posições dos dois.
- Arraste um Pokémon ativo pra dentro do Banco (área vazia do menu) → ele sai da Equipe e vai pro Banco.

Funciona com mouse e com toque (celular).

## Arena (esboço v1 + motor de ataque)

Botão **"🏟️ Arena"** no topo — espaço à parte pra montar uma batalha, feito como primeiro rascunho pra iterar em cima. Modelo de dados novo em `STATE.arena` (`teams` + `entries` + `lastRoll` + `log` + `round`), migração automática pra fichas salvas antes disso existir.

- **Times**: sem limite, criados/renomeados/removidos só pelo Mestre — não precisa ser 1×1, dá pra ter quantos Times a mesa quiser.
- Cada Treinador arrasta (pelo ⠿, mesma engine de Pointer Events da Equipe/Banco) um Pokémon da própria Equipe ativa pra dentro de um Time. A entrada guarda só a referência (Treinador + Pokémon) — o Pokémon continua na ficha normalmente, nada é duplicado ou movido de lá.
- **Equipe ativa é pública, mas cada Pokémon começa em Pokébola**: `arenaSourceHTML()` renderiza o card "Equipe ativa" de **todos** os Treinadores (`STATE.trainers.map(arenaTrainerSourceHTML)`) pra qualquer viewer, jogador ou Mestre — os cards de todo mundo aparecem sempre. Mas cada Pokémon nele só mostra sprite/nome de verdade pra quem for `canControl` (dono ou `SESSION.role==='gm'`) OU já tiver `p.arenaRevelado===true`; pra qualquer outro viewer, vira um tile Pokébola (`arena-source-tile-hidden`) até o Pokémon ir pra um Time pela primeira vez. Controle (`drag-handle`, `arena-source-tile-swappable`) segue restrito a `canControl` do mesmo jeito.
- **Dentro de um Time, revelado é `true` por padrão** (`DT.arenaSetEntry`/arrastar, `DT.arenaSwapIn`/Trocar): um Pokémon aparece pra mesa inteira (sprite, `arenaEntryHTML`) assim que entra por esses dois caminhos, mesmo antes de agir — persistido no próprio Pokémon (`p.arenaRevelado`), então mesmo saindo do Time depois, ele já aparece de verdade na Equipe ativa dali em diante, **pelo resto dessa mesma passagem pela Arena** (`p.arenaRevelado` zera ao sair de vez — `DT.arenaLeave` — ver bullet abaixo; sem esse reset, um Pokémon que já lutou uma vez ficava marcado como revelado pra sempre, vazando o roster inteiro do Treinador em toda Soltura futura). **Exceção**: uma entrada criada por `DT.arenaPickLead` (Soltura, ver bullet dedicado abaixo) nasce com `revelado:false` de propósito — fica em Pokébola até `DT.arenaRevelarTodos`. `migrateState()` **não** força mais essas a `true` (removida a migração antiga que fazia isso — ela existia de quando toda entrada nascia revelada; hoje `revelado:false` é um estado normal e esperado).
- **Motor de turno reescrito (Cap. 86): seleção simultânea + resolução manual em lote por Iniciativa**, substitui de vez a fila sequencial "um de cada vez" + a negociação de Prioridade (ambas removidas — ver "O que ficou pra trás" mais abaixo). `STATE.arena.round` agora é `{number, phase:'selecting'|'resolving', order, meta, actions, resolveOrder, resolveIdx}` — só o Mestre inicia (`DT.arenaStartRound`, "▶️ Iniciar Batalha", calcula `order`/`meta` com `computeInitiativeOrder()`, mesmo critério de sempre: Velocidade Efetiva → Natural → 1d6 em empate, −10 de Paralisia do Cap. 20.4, excluindo `revelado:false`). Na fase `'selecting'`, **todo mundo escolhe a própria ação livremente e ao mesmo tempo** — `DT.arenaSelectActor` libera qualquer entrada que ainda não tenha uma ação registrada em `round.actions` (`arenaEntriesPendingSelection`), sem depender de "vez" nenhuma; o Mestre pode escolher por qualquer uma que ainda não escolheu (nunca reabre uma já escolhida, nem ele). **Cap. 80 — trava real**: sem `STATE.arena.round` ou fora da fase `'selecting'` (antes de "Iniciar Batalha", ou já resolvendo), **ninguém** abre o menu de ação — nem o Mestre; a única oportunidade de agir é durante a seleção de uma Rodada de verdade. Cada escolha (`arenaQueueAction`) só grava o *bundle* da ação (`{kind:'atacar'|'item'|'trocar'|'fugir', ...}`) em `round.actions[entryId]` — nada resolve ainda. Assim que `arenaEntriesPendingSelection(round).length===0` (todo mundo que podia escolher já escolheu), a Rodada passa sozinha pra `'resolving'` (`arenaBeginResolving`); o Mestre também pode forçar isso antes (`DT.arenaForceResolve`, "Resolver Rodada", pra quando alguém sumiu/travou e a auto-resolução nunca ia disparar). `arenaComputeResolveOrder(round)` decide a ordem de resolução: **ações que não são Ataque (Trocar/Item/Fugir) sempre resolvem primeiro** (entre elas, por Velocidade Efetiva — critério estável), **depois** os Ataques, ordenados por Prioridade desc. → Velocidade Efetiva desc. → Velocidade Natural desc. → 1d6 em empate total (`resolveInitiativeTies`) — sem perguntar nada a ninguém: Prioridade simplesmente entra na frente. **Resolução 100% manual (revisado depois do Cap. 86 original, a pedido do usuário — "eu quero que seja manual... o mestre vai avançando de momento em momento")**: `arenaResolveNextStep(round)` executa **um** passo pendente por vez (`arenaExecuteQueuedAction` despacha pro tipo certo — `arenaExecuteQueuedAttack`/`Item`/`Swap`/`Flee`, rodando os mesmos testes de Paralisia/Sono/Congelamento (Cap. 20.4/21/22) e Confusão (Cap. 23) de sempre, só que agora na hora de RESOLVER, não de escolher — isso é o que permite Usar Item pra curar a própria condição bloqueante ANTES do teste obrigatório, Cap. 82: quem escolheu Item nunca passa por esse teste, e Item sempre resolve antes de qualquer Ataque), mas só quando o Mestre chama — não existe timer nenhum agendando o próximo passo sozinho. `DT.arenaResolveStep` (botão **"▶️ Avançar"** no card "🎯 Iniciativa", só GM) chama `arenaResolveNextStep` uma vez por clique, com a mesma trava de sempre contra o dado ainda animando/popup de Ação congelado (`if(SESSION.arenaResolving || DICE_ROLL_ANIM) return;`, Cap. 79) — não deixa adiantar o próximo Pokémon antes do resultado atual assentar na tela de todo mundo. **Não existe mais nenhum ticker** (`ensureRoundResolveTicker`/`checkArenaRoundResolution`/`ROUND_RESOLVE_GAP_MS`/`round.nextStepAt` — todos removidos de vez; a versão original deste capítulo tinha um `setInterval` de 300ms avançando sozinho a cada 3s, substituído por esse clique manual). Quando o `resolveOrder` inteiro termina, `arenaFinishRoundResolution` já monta a Rodada seguinte sozinha, de volta em `'selecting'` (`arenaBuildSelectingRound` — quem estiver `carregando` um Movimento de 2 turnos já nasce com a ação de soltar PRÉ-escolhida ali, sem passar pela seleção de novo) — só precisa "Iniciar Batalha" uma vez; com a batalha já vencida, a Rodada simplesmente termina (`STATE.arena.round=null`) sem abrir outra. Um Pokémon que entra de surpresa numa Rodada já em andamento (Mestre mandando alguém novo pro Time) **não** é mais inserido nela — a antiga regra do Cap. 16 original de "pular a frente de quem ainda não agiu" foi **aposentada** (a fila sequencial que ela dependia não existe mais); ele só participa normalmente a partir da próxima fase de seleção. Uma troca voluntária (Trocar como ação) continua consumindo a ação inteira; o convite de graça pós-U-turn/Volt Switch/Parting Shot ou pós-desmaio (`entry.pendingSwitchOut`/fainted-swap) agora **nunca expira sozinho** — fica disponível (clicável) até alguém de fato trocar, mesmo depois da Rodada virar, já que a resolução em lote não dá mais pra pausar no meio esperando um clique só.
- **Bug real corrigido nesta leva: `arenaCheckVictory` "esquecia" um Time inteiro quando um Treinador tinha entradas em dois Times diferentes** (o Mestre pode arrastar Pokémon do mesmo Treinador pra Times separados na mão) — o cálculo antigo colapsava `trainerId->teamId` num mapa (`teamOfTrainer`), sobrescrevendo pela ÚLTIMA entrada de cada Treinador; se esse Treinador também tivesse uma entrada mais antiga num Time diferente ainda vivo, esse Time desaparecia do cálculo de "quem ainda tem alguém vivo" e podia decretar o Time errado como vencedor. Corrigido pra computar `teamsAlive` direto por entrada (`e.teamId`, sem colapsar), então cada Time é conferido pela vida do Treinador de cada entrada dele, sem apagar um Time por causa de outra entrada do mesmo Treinador em Time diferente.
- **Ação de verdade — clicar no Pokémon, sem botões separados**: não existe mais "Usar" nem "Atacar"/"Agir" como botões próprios — clicar direto no card de um Pokémon num Time (já visível desde que entrou lá) e vivo (`DT.arenaSelectActor`) abre um popup (`arenaActionPanelHTML`, `.arena-action-overlay`/`.arena-action-overlay-card`) com um menu de quatro opções: **Atacar**, **Usar Item**, **Trocar de Pokémon**, **Fugir** (`SESSION.arenaAction`). Antes era um `.card` fixo no fluxo normal da página (precisava rolar até ele, e de novo até o alvo/tile de troca lá em cima); agora é um popup centralizado, maior, que de cara já mostra quem está em campo, **lado a lado, um bloco por Time** (`arenaActionBattlefieldHTML`, `.arena-action-battlefield`/`.arena-action-team-col` — um `<div>` por `STATE.arena.teams`, cada um listando as próprias entries via `arenaEntryHTML` de novo, o mesmo card que já existe no campo de batalha, então a lógica de clique/elegibilidade E o gatilho **"📊"** de abrir o painel de Atributos não são duplicados em lugar nenhum). Quando um Movimento/Item pede um alvo manual, é a própria `arenaEntryHTML` de cada entry (já calculada como `arena-slot-targetable`, via a mesma checagem `pickingTarget`/`eligibleMoveTargets`/`eligibleItemTargets` que o campo de batalha normal usa) que fica clicável ali dentro do popup — sem uma "grade de alvos" separada, é literalmente clicar em quem já está no campo mostrado. Trocar reaproveita `arenaSourceTilesRowHTML(t)` (fatorada de `arenaTrainerSourceHTML`, que agora só chama essa função pros próprios tiles) num bloco à parte pra quem NÃO está em campo (Equipe ativa). Se o Pokémon estiver Confuso, o teste da Confusão (ver abaixo) roda automaticamente antes desse popup abrir. Escolhendo Atacar, aí sim se escolhe o Movimento; o alvo (se precisar de um) vem depois. O motor resolve a Precisão (Cap. 10, com fallback automático pra sempre-acerta em Movimentos sem Precisão definida — inclusive corrigindo fichas salvas de antes dessa checagem existir, via `migratePokemonMoves`), Crítico (Cap. 11), Dano (Cap. 12) e Tipos (Cap. 13) com 1d6, e o Efeito Percentual (Cap. 14) com 1d10 quando o Movimento tiver um efeito de status conhecido — se aplicar, a Condição já entra sozinha na ficha do alvo, respeitando a regra de nunca empilhar duas Condições Principais (Confusão é a exceção, Cap. 20). PV chegando a 0 marca Desmaio (Cap. 19) automaticamente. **Card da Arena mais respirado**: `.arena-slot` ganhou mais largura/espaçamento (78px→84px, gap 12px→16px entre cards) — o aviso **"😵 Escolher próximo"** (Cap. 25/59, texto comprido) saiu de `.arena-turn-badge` (`position:absolute`, pensado pro rótulo curto "▶ Turno" — flutuando por cima acabava cobrindo o PV e a sprite por baixo, reportado com print) e ganhou a própria classe `.arena-fainted-banner`, no fluxo normal do card (não flutua, empurra o resto pra baixo em vez de sobrepor).
- **Popup de Ação fecha assim que a escolha é enviada (Cap. 79, revisado pelo Cap. 86)**: `arenaQueueAction` chama `arenaResetActionState()` logo depois de gravar o *bundle* em `round.actions` — quem escolheu não fica mais olhando um popup congelado esperando o resto da mesa escolher, e depois ainda o Mestre resolvendo a Rodada clique a clique (Cap. 86); o popup e o quadro de dado JÁ NÃO precisam mais dividir a mesma janela, porque escolher deixou de ser o mesmo instante que resolver. Continua valendo o resto do design do Cap. 79: quando o `#diceOverlay` de fato abre pra uma rolagem (`STATE.arena.lastRoll`), ele é sempre a própria animação, numa `div` fora da árvore da ficha — nunca embrulhado atrás de um popup de Ação de ninguém, já que a resolução acontece bem depois de qualquer escolha, disparada só quando o Mestre clica "▶️ Avançar" (`DT.arenaResolveStep`), não mais no mesmo clique de quem escolheu. O `SESSION.arenaResolving`/a view congelada "🎲 Resolvendo…" de `arenaActionPanelHTML` continuam existindo no código só como reforço de defesa em profundidade (`if(SESSION.arenaResolving) return;` nos `DT.arena*` de início de ação) — na prática não disparam mais durante a resolução em lote normal, porque `SESSION.arenaAttacker` já foi limpo antes.
- **Alvo do Movimento (Cap. 8.5)**: cada Movimento respeita sua própria lógica de alvo, vinda do dado oficial (`MOVE_TARGETS`, gerado do `target_id` do PokeAPI) — um Movimento **em si mesmo** (Swords Dance, Rest) resolve na hora, sem pedir clique; um **só aliado** (Helping Hand) ou **só inimigo** (Counter) só deixa clicar em quem for elegível daquele time (`eligibleMoveTargets`); um **de área** (Earthquake, Discharge — categoria `all-other`) atinge sozinho todo mundo daquela categoria de uma vez, podendo incluir aliados do próprio atacante junto com os inimigos; um **de campo** (Stealth Rock) resolve sem calcular dano, só registra o uso (mecânica ainda em aberto, Cap. 25). Pra Movimentos de área, a Precisão/Crítico/Efeito Percentual são **uma rolagem só pro uso inteiro** (mesmo d6/1d10 compartilhado, `resolveMoveUse`), só o Dano é recalculado por alvo — mas se o efeito pega ou não em cada um ainda respeita o Cap. 20 individualmente (um alvo que já tinha Condição não recebe outra, mesmo com a rolagem batendo).
- **Paralisia/Sono/Congelamento automáticos (Cap. 20.4/21/22)**: antes do menu de ação abrir, `arenaStatusActionPreCheck` já testa sozinho quem estiver com uma dessas três — Paralisia (1d6, 1–2 perde a ação), Congelamento (1d6, 1–4 continua, 5–6 descongela e libera o menu no mesmo turno), Sono (1º turno obrigatório sem rolar nada, 2º–3º com 1d6, 5–6 acorda, e acorda garantido depois do 3º turno testado sem rolar de novo — contagem em `p.statusTurns`, mesmo campo do contador de Veneno Grave, nunca usados ao mesmo tempo). Mostra um aviso + log; se perder a ação, o turno avança sozinho (`arenaAdvanceTurn`) sem abrir o menu. Qualquer golpe do Tipo Fogo que acerte com dano descongela na hora (`applyAttackResult`), sem precisar bater com o nome do Movimento.
- **Confusão automática (Cap. 23)**, resolvida logo depois das três acima (mesma ordem do Cap. 24): `arenaConfusionPreCheck` já rola sozinho o 1d6 pra tentar eliminar a Confusão (5–6 termina) e, se continuar, o 1d6 do efeito (1–2 perde a ação e sofre 1 PV — bloqueado se tiver Magic Guard, mas ainda perde a ação; 3–6 age normalmente) — mostra um aviso com os dois resultados e registra no log. Se a ação for perdida, o turno já avança sozinho sem abrir o menu.
- **Recuo, Dreno e Movimentos de cura (Cap. 25)**: `applyMoveSelfHealEffects` (chamado de `resolveMoveUse`, depois de `applyAttackResult`/`applyAbilityHooksAfterHit`) lê três tabelas curadas com o dado oficial da PokeAPI (`meta.drain`/`meta.healing`) — `MOVE_RECOIL` (11 Movimentos, % do dano causado tirado do próprio atacante), `MOVE_DRAIN` (11 Movimentos, % do dano causado curado no atacante — vira dano nele em vez de cura se o alvo tiver Liquid Ooze) e `MOVE_HEAL` (15 Movimentos incluindo Rest, % do PV Máximo de quem for o alvo resolvido do golpe). Magic Guard bloqueia o recuo e o dano invertido de Liquid Ooze (dano indireto no próprio atacante), sem bloquear a cura em si. Rest (`MOVE_EFFECTS['Rest']`, curado à mão — a PokeAPI não marca ele com nenhum ailment) cura qualquer Condição anterior antes de aplicar o próprio sono, e cura 100% do PV via `MOVE_HEAL`.
- **Movimentos que trocam depois de atacar (Cap. 25)**: `SWITCH_OUT_MOVES` (U-turn, Volt Switch, Parting Shot — lista curada, a PokeAPI não marca isso em nenhum campo) — em `DT.resolveArenaMove`, se o atacante não desmaiou nele mesmo (`info.attackerFainted`, setado no fim de `resolveMoveUse` a partir do PV real dele) e sobra alguém na Equipe ativa (`arenaHasSwapCandidate`), a troca vira a continuação obrigatória da mesma ação: abre o painel de troca (`SESSION.arenaAction='trocar'`, `SESSION.arenaForcedSwitch=true` — sem botão Cancelar, é obrigatório) sem passar a vez ainda; `DT.arenaSwapIn` (mesma função de sempre) cuida do dano de status de fim de turno e de avançar a Rodada quando o Treinador escolher quem entra. Sem candidato, passa a vez normal.
- **Movimentos de carregar 2 turnos (Cap. 25)**: `CHARGE_MOVES` (16 "Movimentos de dois turnos" oficiais curados à mão — Bounce/Dig/Dive/Electro Shot/Fly/Freeze Shock/Geomancy/Ice Burn/Meteor Beam/Phantom Force/Razor Wind/Shadow Force/Sky Attack/Skull Bash/Solar Beam/Solar Blade — a PokeAPI não marca isso em nenhum campo, `category:'damage'` igual golpe comum) + `CHARGE_MOVE_MSG` (frase de sabor por Movimento). `DT.resolveArenaMove` ganhou um 5º parâmetro `isChargeRelease`: sem ele, um `CHARGE_MOVES[move.nome]` sem `poke.carregando` ainda pendente trava `{moveIdx,moveName,category,targetIds}` no atacante (PP já descontado, sem Precisão/Crítico/dano nenhum rolando) e passa a vez, em vez de resolver de verdade. `DT.arenaSelectActor`, logo depois dos pré-testes de Paralisia/Sono/Congelamento/Confusão, checa `p.carregando` primeiro: se estiver marcado, solta o golpe sozinho (recalcula quem ainda é elegível pra Movimento de alvo manual — se ninguém sobrou, o golpe se perde sem efeito mas o turno passa igual; self/área sempre resolvem frescos, sem travar alvo) chamando `DT.resolveArenaMove(..., true)` — o `isChargeRelease=true` pula o desconto de PP (já foi pago no turno de carga) E o "if" de início de carga (senão a 2ª chamada, já com `carregando` limpo pelo próprio `arenaSelectActor` antes de chamar, cairia de novo nele — carregando pra sempre em vez de golpear) — sem passar pelo menu Atacar/Item/Trocar/Fugir de novo. `resetStages(poke)` também zera `poke.carregando` (mesmos pontos de saída de campo do Estágio, Cap. 15.1) — sair de campo no meio da carga perde o golpe pendente. Selo **🌀** no card (`arenaEntryHTML`) e linha "Carregando: X" no painel de atributos (`arenaStatsPanelHTML`, Cap. 15.1) enquanto ativo. Fica de fora, por enquanto: a semi-invulnerabilidade de Fly/Dig/Dive/Bounce/Phantom Force/Shadow Force durante a carga, e o Power Herb.
- **Dano de status por turno, automático (Cap. 20)**: assim que o Pokémon que agiu termina a ação (ataque, item, troca ou fuga — `arenaAdvanceTurn`), se ele tiver Queimadura (−1 PV), Veneno (−2 PV) ou Veneno Grave (crescente 1→2→3...) o dano já é aplicado sozinho e entra no Registro, mesmo fora de uma Rodada de Iniciativa ativa.
- **Barra de PV com número atual/máximo**: `hpBits`/`arena-hp-label` renderiza a barra de PV **acima** do sprite (não mais abaixo), com o texto `cur/max` por cima dela — tanto em `arenaEntryHTML` (Time) quanto em `arenaTrainerSourceHTML` (Equipe ativa, só nos tiles visíveis).
- **Palco de batalha (cena visual de arena) — testado e revertido**: chegou a existir uma cena visual acima dos cards de Times (RPG) e dos mini-cards (PVP) — estádio em perspectiva, sprites de costas/de frente, animações simples de golpe — passou por três rodadas de refinamento a partir de imagens de referência do usuário, mas no fim o usuário decidiu que não gostou do resultado ("Pode tirar a arena gráfica, gostei não... vamos voltar como estava antes") e pediu pra tirar dos dois lugares (RPG e PVP), guardando a ideia pra talvez retomar depois. Removida por completo (`battleStageHTML` e toda a família de funções/CSS `.battle-stage-*`); a Arena e a Área PVP voltaram a mostrar só os cards de sempre. O histórico completo de como foi construída (trigonometria da "mesa redonda", trapézio de perspectiva forçada, etc.) fica preservado no histórico do git, não repetido aqui.
- **Mochila (itens) com tipo e cura de verdade, em 5 abas**: cada Treinador tem uma lista de itens na própria ficha, gerenciada só pelo Mestre (`DT.addCatalogItem`/`addItem`/`removeItem`/`setItemQty`); aparece na Arena — cada um vê a própria, o Mestre vê a de todo mundo. `inventoryHTML(t, canGM, mode)` renderiza como uma tabela real (Nome/Descrição/Quantidade, + coluna "Usar" pra `mode==='own'||mode==='gm'`, + coluna de remover pro Mestre) dentro de **5 abas** (`MOCHILA_TABS = ['pocoes','pokebolas','missao','tm','geral']`, rótulos em `MOCHILA_TAB_LABELS`, estado em `SESSION.mochilaTab`, `DT.setMochilaTab`) — `mochilaTabForItem(it)` decide a aba pela `categoria` do item via `MOCHILA_TAB_CATEGORIAS` (Poções/Curas de Status/Revives/Berries → Poções e Berries; Pokébolas → Pokébolas; Itens-Chave → Itens de Missão; TMs → TM; qualquer outra categoria, incluindo em branco/homebrew → Inventário Geral, aba catch-all). Cada item tem um `tipo` (`'vida'`, `'condicao'`, `'revive'`, `'ambos'`, `'pp'` ou `'outro'`, `newItem`, `ITEM_TIPOS_VALIDOS`) — Vida cura `cura` PV (0 = cura total), Condição cura `condicaoAlvo` (`''` = qualquer, tipo Full Heal; `'veneno'` cura Veneno e Veneno Grave), Reviver (`reviveFull`) reanima um Pokémon desmaiado devolvendo `cura` PV (0 = total), Ambos faz Vida+Condição juntos (tipo Full Restore), **PP** (`ppAlvo`/`ppFull`/`ppUp`, ver bullet de PP abaixo) recupera/aumenta PP, e **Outro** (pedras, TMs etc. — Pokébolas também são `tipo:'outro'`, ver bullet do botão Usar abaixo pra a exceção delas) não tem efeito mecânico de verdade ainda (Cap. 25) — tudo aplicado por `applyItemCure`/`describeItemCureResult` (compartilhado entre a Cura da ficha e a Arena; `applyItemCure` devolve `{tipo:'outro'}` incondicionalmente, sem checar PV/Condição do alvo). **Trava de desmaiado**: `eligibleItemTargets` só deixa Vida/Condição/Ambos mirar quem não está desmaiado e só deixa Reviver mirar quem está; PP e Outro miram qualquer um — os bloqueados retornam `bloqueado:'desmaiado'|'nao_desmaiado'` em vez de aplicar. No menu de ação, **Usar Item** já separa a lista por tipo (`ITEM_TIPO_LABELS`) e filtra o alvo (o próprio Pokémon ou um aliado) por essa trava — itens `'outro'` continuam só descontando 1 unidade e registrando no log, sem efeito (Cap. 25).
- **Catálogo de itens oficiais por categoria, com descrição, sempre por inteiro** (`ITEM_DEX`, 417 itens; `ITEM_CATEGORIAS`, 11 categorias — Poções, Curas de Status, Revives, Pokébolas, Itens de Batalha, Itens de Evolução, Berries, Itens Segurados, Itens-Chave, Recuperação de PP, TMs): dados reais convertidos do jogo oficial (nomes em inglês), fonte [PokeAPI](https://pokeapi.co/). Todo item tem um campo `descricao` (traduzida à mão pro português — a PokeAPI não tem idioma PT nenhum pra item/Movimento —, com o valor de cura de item tipo Vida citando o número real do NOSSO sistema, não o oficial do jogo; texto original vinha de `short_effect`/`flavor_text` da PokeAPI, com acerto de acentuação via NFD-normalize + um mapa `SLUG_FIX` pra slugs irregulares tipo Upgrade→up-grade); os tipos com mecânica de verdade (Vida/Condição/Reviver/Ambos/PP) têm também `cura`/`condicaoAlvo`/`reviveFull`/`ppAlvo`/`ppFull`/`ppUp` reais, o resto entra como `'outro'`, só catalogado (inclui as 229 TMs vigentes de Scarlet/Violet, achadas via `machines` de cada Movimento na PokeAPI, nome formatado "TM001"–"TM229", com descrição em português via template "Ensina X a um Pokémon compatível."). `findItemByName` busca por nome exato; `itemDexAtivo()` sempre devolve `ITEM_DEX` inteiro — o antigo toggle de categoria por mesa (`STATE.itemCategoriasDesativadas`/`DT.toggleItemCategoria`/o card "🎒 Categorias de Itens" no Painel do Mestre) foi **removido**: confundia com as 5 abas da Mochila (que já organizam por categoria, sem esconder nada) e não tinha razão de existir depois delas.
- **"+ Adicionar" da Mochila: só escolher o item e clicar**: nada de 8 campos soltos sempre visíveis — é um `<select>` com `<optgroup>` por categoria, populado só com o que `itemsForMochilaTab(activeTab)` devolve pra aba aberta (filtra `itemDexAtivo()` por `mochilaTabForItem`, então a lista nunca mostra os ~415 itens de uma vez, só os da aba certa) e um botão "+ Adicionar". `DT.addCatalogItem(tid, selectEl)` aplica de fato (Tipo/PV/Condição/Reviver/PP/Descrição/Categoria 100% do `ITEM_DEX`, sem nenhum campo manual, sempre **+1 unidade** por clique — mais que isso, o Mestre ajusta na própria linha da tabela depois) e só então chama `mutate()`. Um botão **"✏️ Item personalizado"** (`DT.toggleMochilaCustom`, manipula só `style.display` direto no elemento, sem `render()`) esconde/mostra o formulário antigo de campos soltos — mantido só pra item fora do catálogo (homebrew) ou pra sobrescrever o oficial à mão — com `DT.addItem`/`DT.autofillNewItem` por baixo, exceto que `DT.autofillNewItem` **parou de chamar `render()`/usar o banner `SESSION.notice`** (escrevia no formulário e imediatamente apagava, porque `render()` reconstrói `#app.innerHTML` do zero a cada chamada — `document.getElementById('app').innerHTML = screenHTML()` — e qualquer valor de campo que não vem de `STATE`/`SESSION` se perde no instante seguinte) e agora escreve só num `<p id="newItemAutofillNote_TID">` local.
- **Botão "▶️ Usar" por linha pula direto pra "qual Pokémon" — sem aba nem lista** (`curaHTML`, fora de batalha, reescrita nesta rodada): `DT.mochilaUseItem(tid, itemId)` seta `SESSION.curaOpenFor`/`curaItemId` (não existe mais `SESSION.curaTab`) e chama `render()` — `curaHTML(t, mode)` agora só renderiza quando `SESSION.curaOpenFor===t.id` E `SESSION.curaItemId` aponta pra um item de verdade daquele Treinador; sem isso, devolve `''` (nada). Não existe mais botão solto "🩹 Cura" nem abas Vida/Condição/etc. pra navegar dentro do painel — `DT.toggleCura`/`DT.curaSetTab`/`DT.curaPickItem` foram **removidos** (o "escolher o item" já aconteceu no clique do "Usar"; a única navegação restante é escolher o Pokémon, e pro PP de um Movimento só, o Movimento). O filtro de Pokémon elegível (`semRestricao = item.tipo==='pp' || item.tipo==='outro'`) deixa escolher qualquer um, mesmo desmaiado, pra PP/Outro. Pra um item de PP `ppAlvo:'um'` (Ether/Max Ether/PP Up/PP Max), um 2º passo (`SESSION.curaPokeId`, `DT.curaPickPoke`/`curaBackToPoke`) pede qual dos 4 Movimentos recebe. `DT.curaApply(tid, pokemonId, moveIdx)` aplica `applyItemCure` direto, sem precisar da Arena — desconta a unidade e, se `!result.bloqueado`, fecha o painel inteiro (`curaOpenFor`/`curaItemId`/`curaPokeId` todos voltam a `null`; um resultado `bloqueado` — ex. Revive em quem não desmaiou — deixa o painel aberto pra tentar outro Pokémon sem precisar clicar "Usar" de novo). `DT.curaCancelPick()` também fecha tudo (antes só limpava o item). Só o dono (`mode==='own'`) ou o Mestre (`mode==='gm'`) veem o botão "Usar" e o que ele abre.
- **Pokébola sem "Usar" — sem sistema de captura ainda, Cap. 25**: nova função `itemUsavelEmEquipe(it)` (`return it.categoria !== 'Pokébolas'`) — Pokébola é pra capturar um Pokémon selvagem/alheio, não faz sentido oferecer "usar" numa Pokémon que já é do próprio Treinador. `inventoryHTML` só desenha o botão "▶️ Usar" quando `itemUsavelEmEquipe(it)` também é verdadeiro (além de `quantidade>0`); `DT.mochilaUseItem` recusa (defesa em profundidade, mesmo chamada direto); na Arena, `DT.arenaPickItem` recusa igual, e a lista do menu "Usar Item" (`arenaActionPanelHTML`) já filtra por `itemUsavelEmEquipe` na montagem dos grupos por tipo — então uma Pokébola simplesmente não aparece pra escolher, nem lá nem na Mochila. Qualquer outro item `'outro'` (pedra de evolução, item segurado, TM etc.) continua liberado normalmente.
- **Naturezas** (`NATURES_DEX`, as 25 oficiais, `{nome,mais,menos}`): cada Pokémon tem `natureza` (Mestre só, `DT.setNatureza`) — `natureModifier` aplica +1/-1 automático no Atributo Efetivo correspondente (nunca em PV) dentro de `effectiveStat`; as 5 naturezas neutras (mesmo `mais`/`menos`) não alteram nada.
- **Habilidades** (`ABILITIES_DEX`, 373 entradas reais `{name,efeito}` via PokeAPI; `SPECIES_DEX` ganhou o campo `hab:[{n,h}]` por espécie, `h`=Habilidade Oculta): cada Pokémon tem `habilidade` (Mestre só, `DT.setHabilidade`) restrita às opções de `habilidadesDaEspecie(p.especie)` (validação, nunca muda) — trocar a Espécie (`DT.setPokeField`) limpa a Habilidade se ela não existir mais na nova espécie. `findAbilityByName` mostra o efeito oficial como legenda da que já está escolhida.
- **Descrição de TODA Habilidade candidata, não só da já escolhida** (pedido do usuário — "fica complicado de escolher sem isso"): `abilityChoicesRefHTML(names)` renderiza, logo abaixo do `<select>`, uma lista de referência com Nome + descrição oficial (`findAbilityByName`) de cada opção do seletor — antes tinha que escolher, ler o efeito, trocar, ler de novo, pra comparar. Uma função só, usada pelos dois seletores de Habilidade que existem (`habilidadeRow` em `pokeCardHTML`, Mestre no RPG; `habilidadeRow` em `pvpFichaCardHTML`, jogador na Área PVP) — nunca uma lista com descrição e a outra sem.
  - **93 delas têm gatilho mecânico de verdade** (`ABILITY_MECH_SET`, escolhidas pra cobrir ≥1 Habilidade mecânica em 85,6% dos 1025 Pokémon sem depender de clima/terreno/item equipado): `habilidadesMecanicasDaEspecie`/`habilidadesSelecionaveisDaEspecie` filtram o seletor do Mestre pra elas quando a Espécie tem alguma, caindo pra lista completa (só referência) senão. Motor: `moveMakesContact`/`NO_CONTACT_PHYSICAL` (aproximação de golpe físico = contato, a PokeAPI não expõe isso), `PUNCH_MOVES`/`BITE_MOVES`/`SOUND_MOVES` (listas curadas), `abilityTypeAbsorb` + os blocos de Mold Breaker/Wonder Guard/Soundproof/Scrappy/Tinted Lens dentro de `resolveAttack` (imunidade/absorção/resistência de Tipo), bônus de dano do atacante e Sturdy também em `resolveAttack`, `abilityFlatStatBonus`/`effectiveStatIgnoringStage` (Guts/Hustle/Huge Power/Pure Power/Defeatist/Protosynthesis/Quark Drive/Unaware, dentro de `effectiveStat`), `applyAbilityHooksAfterHit` (contra-ataque no contato, Synchronize, Poison Touch, Aftermath, Moxie/Beast Boost — chamado de `resolveMoveUse`), `applyOnSwitchInAbility`/`applyOnSwitchOutAbility` (Intimidate + `INTIMIDATE_BLOCK`/`INTIMIDATE_REVERSE`, Download, Regenerator, Natural Cure — chamados de `DT.arenaSetEntry`/`arenaEnter`/`arenaSwapIn`/`arenaLeave`), o fim de `applyEndOfTurnStatusAndLog` (Speed Boost, Shed Skin, Ice Body/Rain Dish, Healer, Magic Guard, Poison Heal), e Pressure (dobra o custo de PP, ver abaixo, checada direto em `DT.resolveArenaMove`). `STATUS_IMUNE_HABILIDADE`/`STATUS_IMUNE_TOTAL`/`abilityBlocksStatus` cobrem as imunidades de Condição. As outras ~280 continuam só texto de referência, sem gatilho — igual era antes dessa leva.
- **PP (Cap. 8.6), travado pro jogador + itens de recuperação (Cap. 8.6.1)**: `MOVES_DEX` ganhou o campo `pp` (PP oficial de cada um dos 847 Movimentos, via PokeAPI). Cada slot de Movimento do Pokémon tem `pp` (Máximo), `ppAtual` e `ppBase` (teto original, nunca muda sozinho — só quando o Mestre edita o Máximo à mão, via `DT.setMove` field `'pp'`, que sincroniza os dois) — `applyMoveData` preenche os três sempre que um Movimento novo é escolhido/digitado, tanto pelo Mestre (`DT.setMove`) quanto pelo Treinador via Learnset (`DT.setMoveSelect`); `migratePokemonMoves` faz o mesmo backfill (idempotente) em fichas salvas de antes desses campos existirem. `DT.setMovePP` **agora é só do Mestre** (`if(SESSION.role!=='gm') return;`) — o Treinador só vê "atual / máximo" como texto, sem input. Na Arena, `DT.arenaPickMove` recusa escolher um Movimento com `ppAtual<=0` quando `pp>0` (Movimento sem PP cadastrado, ex. homebrew que não bateu com a base, nunca bloqueia); `DT.resolveArenaMove` desconta 1 PP, ou 2 se qualquer alvo resolvido (incluindo o próprio usuário, num Movimento `self`) tiver a Habilidade Pressure; `DT.resolveFieldMove` também desconta 1. Sem Struggle automático — 0 PP nos 4 Movimentos é decisão do Mestre. Seis itens novos no `ITEM_DEX` (categoria "Recuperação de PP" — Ether, Max Ether, Elixir, Max Elixir, PP Up, PP Max) recuperam/aumentam PP automaticamente via `curaPPUm(item, mv)`: sem `ppUp`, soma `ppFull ? até o Máximo : +10` no Atual; com `ppUp`, aumenta o Máximo em 20% do `ppBase` por vez (`ppFull` aplica de uma vez o teto de +60%, equivalente a PP Max), refletindo o mesmo ganho no Atual. `applyItemCure` ganhou um 4º parâmetro `moveIdx` e um branch `tipo:'pp'` no topo que resolve `ppAlvo:'todos'` (loop nos 4 Movimentos, Elixir/Max Elixir) ou `'um'` (usa `moveIdx`, Ether/Max Ether/PP Up/PP Max) — usado tanto pela aba Cura (`DT.curaApply`) quanto pela Arena, onde um novo passo intermediário (`SESSION.arenaItemTargetId`, `DT.arenaPickItemMove`) pede qual Movimento recebe antes de resolver `DT.resolveItemUse`.
- **Estágios automáticos por Movimento (Cap. 15), travados pro jogador**: nova tabela `MOVE_STAT_EFFECTS` (172 Movimentos oficiais com `stat_changes`, cada entrada `{changes:[{stat,change}],chance,self?}`) — `self:true` marca os 27 Movimentos de dano com efeito colateral no próprio atacante (ex. Close Combat), identificados manualmente por não terem como o campo `target` da PokeAPI diferenciar "efeito no atacante" de "efeito no defensor" quando `meta.category==='damage-raise'`. `resolveAttack` rola um 1d10 próprio (`estagioD10a`/`estagioD10b`, independente do d10 de ailment, com dobra de Serene Grace) e devolve `result.estagioEfeito`; `applyAttackResult` (ganhou um 4º parâmetro `attPoke`, usado pra calcular `fromOpponent = attPoke && defPoke!==attPoke`) aplica no atacante se `self`, senão no defensor — reaproveitando `INTIMIDATE_BLOCK`/`INTIMIDATE_REVERSE`/Defiant/Competitive (Cap. 6.3) pra qualquer queda vinda de oponente, não só Intimidate; Shield Dust só bloqueia com `fromOpponent` verdadeiro (corrigido um bug latente em que ele também bloqueava o Pokémon usando um Movimento nele mesmo). `resetStages(poke)` zera os 7 Estágios; chamado em todo caminho de saída de campo — desmaio (`applyAttackResult`, `applyEndOfTurnStatusAndLog`, `arenaConfusionPreCheck`, hooks de contra-ataque em `applyAbilityHooksAfterHit`), troca (`DT.arenaSwapIn`, no Pokémon que sai), fuga bem-sucedida (`DT.arenaFlee`) e remoção manual (`DT.arenaLeave`, `DT.arenaRemoveEntry`). `DT.setStage` agora só funciona pro Mestre (`if(SESSION.role!=='gm') return;`); no card do Pokémon, os inputs de Estágio (`stageFields`) só aparecem editáveis pra `canGM`, nunca mais pro Treinador.
- **Trocar de Pokémon (Cap. 18), clicando direto na Equipe ativa**: escolher Trocar não pede pra arrastar nada — destaca (`arena-source-tile-swappable`) os Pokémon da Equipe ativa daquele Treinador que estão vivos e ainda não são uma entrada ativa em outro lugar; um clique chama `DT.arenaSwapIn(pokemonId)`, que aplica o dano de fim de turno pendente no que está saindo (é a última ação dele em campo), remove a entrada antiga da Arena por completo (volta a existir só na Equipe ativa) e cria uma entrada nova pro escolhido, já revelada, no mesmo Time — consome a ação inteira, o que entrou só age a partir do turno seguinte. Esse mesmo caminho (`SESSION.arenaAction='trocar'`) também é reaproveitado por U-turn/Volt Switch/Parting Shot (forçado, `arenaForcedSwitch:true`, sem Cancelar) e pelo convite de "escolher próximo" depois de um Desmaio (`DT.arenaSelectFainted`, não forçado — tem Cancelar, ver bullet de Desmaio abaixo).
- **Desmaio e vitória automática (Cap. 19/25)**: `arenaCheckVictory` roda depois de qualquer coisa que possa zerar o PV de alguém (`arenaAdvanceTurn`, as duas pontas de `DT.resolveArenaMove`, fim de `DT.arenaSwapIn`) — confere se só sobrou um Time com algum Treinador ainda tendo Pokémon vivo (`t.pokemons`, current HP > 0); se sim, seta `STATE.arena.vencedor` (id do Time), loga, e a partir daí `DT.arenaSelectActor` recusa qualquer ação nova até o Mestre limpar (`DT.arenaClearVictory`, só ele) — banner "🏆 [Time] venceu a batalha!" (`arenaVictoryHTML`) pra mesa toda. Antes disso, se um Pokémon desmaiar mas o dono ainda tiver outro vivo na Equipe ativa, o próprio slot desmaiado vira um convite clicável (`arena-slot-fainted-swap`, `DT.arenaSelectFainted`) que abre o mesmo seletor de troca de sempre, sem passar pelo menu Atacar/Item/Trocar/Fugir.
- **Fugir (Cap. 18.1), com trava do Mestre e 1d10**: só o Mestre liga/desliga a **🔒 Trava de Fuga** (`STATE.arena.fugaTravada`, `DT.arenaToggleFugaLock`) pra batalha inteira — representa algum Movimento/Habilidade/motivo que prenda os Pokémon em campo; travada, `DT.arenaFlee` bloqueia na hora, sem gastar a ação nem rolar dado. Sem trava, rola **1d10**: 1–3 a fuga falha (a ação é gasta, o Pokémon continua em campo, o turno passa) e 4–10 ela funciona (a entrada some da Arena de vez) — os dois casos ficam no Registro.
- **Fluxo guiado de entrada — Time → Soltura → Revelação simultânea (Cap. 17/55)**, redesenho completo do "Entrar em massa" antigo: `DT.arenaEnterPick`/`DT.arenaEnter` agora só registram presença (`STATE.arena.presentes`, array de `{trainerId,teamId}` — mesmo padrão de `spectators`), no Time escolhido (auto-escolhe se só existir 1; com 2+, `SESSION.enterArenaTeamPick` mostra um seletor inline) — **não** cria nenhuma entrada de combate ainda. `DT.arenaOpenSoltura` (só o Mestre) libera `STATE.arena.solturaAberta`; a partir daí, `DT.arenaPickLead(trainerId, pokemonId)` deixa cada presente soltar **um** Pokémon da própria Equipe ativa, criando a entrada já com `revelado:false` (sem chamar `applyOnSwitchInAbility` ainda, sem marcar `p.arenaRevelado` — segredo de verdade, nem a Equipe ativa pública vaza) — só um por Treinador nessa fase, `DT.arenaCancelPickLead` desfaz antes de revelar. `DT.arenaRevelarTodos` (só o Mestre, só quando ninguém que está presente falta escolher) revela todo mundo **em duas passadas**: primeiro marca `revelado`+`arenaRevelado` de todos os que soltaram nessa leva, só depois dispara `applyOnSwitchInAbility` de cada um — assim uma Intimidate que revele junto já enxerga todo mundo que revelou na mesma leva, igual uma entrada simultânea de verdade. `arenaSolturaHTML` mostra quem já soltou (✅/⏳) pra mesa toda, sem dizer qual Pokémon. **Trava de segurança**: `DT.arenaSelectActor` recusa abrir o menu de ação numa entrada `revelado:false` pro próprio dono (furaria a revelação simultânea — só clicar já revelava sozinho antes dessa correção); só o Mestre pode agir com uma entrada em segredo, e nesse caso ela é revelada ali (correção manual de mesa). Arrastar (`DT.arenaSetEntry`) continua sendo um caminho à parte, sempre revelado na hora — ferramenta de ajuste manual, não o fluxo guiado. `DT.arenaLeave` remove presença + qualquer entrada daquele Treinador de uma vez, de qualquer Time — e (correção deste bug) também zera `p.arenaRevelado` de **todo** o time dele (`t.pokemons.concat(t.banco)`, não só de quem estava em campo): sem isso, a marca de revelado nunca era resetada em lugar nenhum do código, então um Pokémon que lutasse em qualquer batalha passada continuava aparecendo com sprite/nome de verdade na Equipe ativa pra qualquer não-dono em **toda** batalha futura também, furando o segredo de uma Soltura nova antes mesmo dela abrir.
- **Modo Espectador**: `STATE.arena.spectators` (array de trainerId) + `DT.arenaToggleSpectator` — presença pura, sem Pokémon nenhum envolvido; qualquer Treinador entra/sai da própria conta (GM alterna qualquer um). `arenaSpectatorHTML()` mostra o botão pro jogador logado e a lista "👀 Assistindo" com quem está no modo, pra mesa toda.
- **Overlay de dado**: todo mundo conectado vê um quadro no centro da tela com o resultado (`STATE.arena.lastRoll`), numa div própria fora da árvore da ficha pra não recriar sprite nenhum a cada quadro da animação. O **d6** gira e assenta primeiro; só então, se houver Efeito Percentual, o **1d10** gira e assenta (com uma 2ª tentativa em sequência se veio Crítico e a 1ª falhou) — um dado de cada vez, nunca simultâneos. Some sozinho depois de alguns segundos. Uma rolagem só dispara esse overlay se aconteceu há pouco (`DICE_FRESH_MS`, 8s) — corrige um bug do v1 em que reabrir/recarregar a página replicava o overlay pra um ataque antigo já salvo no banco.
- **Registro**: `arenaLogHTML()` guarda as últimas 40 linhas de ataque/status/troca/item resolvidas (`STATE.arena.log`), rolável, com botão "Limpar" pro Mestre. A entrada mais nova só aparece ali depois que o dado dela termina de girar no overlay, pra não entregar o resultado antes da hora. Janela flutuante (`.arena-log-panel`), arrastável pela barra do topo e redimensionável pelo cantinho `.arena-log-resize-handle` — mesmo motor de arraste da `.arena-stats-panel` (offset do ponteiro, mexe direto no `style.left/top` fora do ciclo de `render()`), com um motor irmão de resize (`LOG_RESIZE`) que pina o painel em `left/top` explícitos assim que o resize começa (senão a borda ancorada por `bottom`/`right` do CSS padrão se deslocaria sozinha crescendo). Posição (`SESSION.arenaLogPos`) e tamanho (`SESSION.arenaLogSize`) são só locais, por viewer, nunca sincronizam — igual ao painel de atributos.
- **Etiqueta de efetividade de Tipo nas rolagens (Cap. 13)**: `resolveAttack` guarda `result.typeMod` = o `mod` puro de `typeModifier(move.tipo, defTipo1, defTipo2)`, capturado ANTES de qualquer Habilidade mexer em `tm.mod` (Thick Fat -2, Tinted Lens zerando negativo, etc.) — assim a etiqueta reflete só a conta real do Tipo, sem ruído de Habilidade; só setado quando `isDamaging` (Físico/Especial — Status nunca ganha a etiqueta). `typeEffectivenessLabel(typeMod, immune)` (perto de `typeModifier`) devolve `'Super Efetivo (x2)'`/`'(x4)'`, `'Pouco Efetivo (x1/2)'`/`'(x1/4)'`, `'Imune'` (prioridade sobre qualquer mod) ou `null` (neutro — sem etiqueta, igual sempre foi). `STATE.arena.lastRoll.alvos[i].typeMod` carrega o valor por alvo (`DT.resolveArenaMove`); `rollLogLine` (Registro) e `rollSummaryLines` (quadro de dado) chamam a mesma função pra não duplicar a lógica entre os dois lugares.
- **Indicativo visual de Estágio (Cap. 15.1)**: `stageBadgesHTML(poke)` desenha uma etiqueta tipo "Atq +1"/"Vel -2" (`.stage-badge-up` verde/`.stage-badge-down` vermelha) por Atributo com `poke.estagios[stat]!==0`, embaixo de qualquer card revelado na Arena (`arenaEntryHTML`) — cobre os 5 Atributos normais mais Precisão/Evasão, sempre lendo o Estágio real (o mesmo que já alimentava `effectiveStat`/o motor de ataque; só faltava aparecer em algum lugar durante a própria batalha).
- **📊 Painel flutuante de atributos**: botão `.arena-info-btn` em todo card revelado (`arenaEntryHTML`) chama `DT.arenaStatsShow(entryId)`, que empurra `entryId` pra `SESSION.arenaStatsOpen` (array — refazendo a mesma checagem de `canSeeReal` — GM, dono ou já revelado — e sem duplicar se já estiver aberto) e dispara `arenaStatsPanelHTML()`, que mapeia CADA entryId aberto pra um painel via `arenaStatsOnePanelHTML(entryId, idx)`: Tipo, PV atual/máximo, Condição/Confuso, Habilidade (com o efeito como legenda), Natureza e uma tabela Atributo/Estágio/Efetivo calculada com o `effectiveStat(t,p,stat)` de verdade (mesma função que a batalha usa), mais os Movimentos com PP restante. Dá pra ter **vários painéis abertos ao mesmo tempo, de Pokémon diferentes** — cada `<div class="arena-stats-panel" data-entry-id="...">` carrega o próprio id, e `DT.arenaStatsClose(entryId)` fecha só aquele, sem mexer nos outros. Sem posição arrastada salva ainda, cada painel nasce em cascata (`top:70+idx*36px; right:16+idx*20px`) pra não empilhar exatamente um em cima do outro. **Arrastável**: `onpointerdown="DT.arenaStatsDragStart(event)"` na barra do topo do painel liga um motor de arraste próprio e simples (`STATS_DRAG` + `DT._statsDragMove`/`DT._statsDragEnd`, mexendo direto em `style.left/top` fora do ciclo de `render()` enquanto arrasta, lendo `data-entry-id` do painel pra saber em qual chave gravar a posição final só no soltar) — sem ghost nem drop-target, diferente do `DRAG` usado pra arrastar Pokémon entre Equipe/Banco/Time. Tudo isso vive só em `SESSION` (`arenaStatsOpen` — array de entryId; `arenaStatsPos` — mapa entryId→{x,y}), que **nunca sincroniza** com o Firestore — cada pessoa na mesa abre, arrasta e fecha os próprios painéis sem afetar os outros. O botão `.arena-stats-close` (✕) e o "Limpar" do Registro (`.arena-log-panel`) carregam `onpointerdown="event.stopPropagation()"` além do próprio `onclick` — sem isso, o `pointerdown` do clique borbulha primeiro pra barra do topo, cujo handler chama `preventDefault()`+`setPointerCapture()` (pra iniciar o arraste); isso desvia o `mousedown`/`mouseup` sintetizados pro elemento que capturou o ponteiro, e o "click" do navegador acaba disparando na BARRA em vez de no botão — o `onclick` do botão nunca roda (bug relatado: "clico e não fecha").
- **Negociação de Prioridade (Cap. 17.1-17.3) — REMOVIDA (Cap. 86)**: o popup "Seu oponente usou X... Deseja usar Y?" (`STATE.arena.priorityPending`/`priorityPausedAction`, `DT.arenaPriorityAnswer`, `findBestPriorityReaction`, `.priority-overlay`) deixou de existir — pedido explícito do usuário ("nem precisa da função de selecionar se quer ou não usar a prioridade"). Prioridade continua decidindo quem age primeiro entre Ataques, só que sozinha, dentro de `arenaComputeResolveOrder` (ver o bullet do motor de turno acima) — sem pausar nada nem perguntar a ninguém no meio do caminho.
- **Cap. 80/82/83/84/85/87 (mesma leva do Cap. 86)**: **trava de ação real** — sem uma Rodada em fase `'selecting'`, ninguém age, nem o Mestre (`DT.arenaSelectActor`, ver o bullet do motor de turno acima; antes disso um jogador conseguia abrir o menu de ação mesmo antes de "Iniciar Batalha"). **Usar Item cura a própria condição bloqueante a tempo** — o teste obrigatório de Paralisia/Sono/Congelamento (`arenaStatusActionPreCheck`) e de Confusão só rodam dentro de `arenaExecuteQueuedAttack`, nunca pra quem escolheu Item; como Item sempre resolve antes de qualquer Ataque na mesma Rodada (ordem do Cap. 86), curar o Sono e depois "acordar e agir normalmente" no mesmo turno não é possível (só 1 ação por Rodada) — mas destrava a Rodada seguinte de verdade, sem o Pokémon nunca ter sido pulado à força; a cura consome a ação inteira da Rodada, igual qualquer outro uso de Item. **Popup de Ação restrito ao próprio Time** — já era assim por construção (`SESSION` nunca sincroniza; `arenaActionPanelHTML` só existe pra quem chamou `DT.arenaSelectActor` na própria sessão, e `DT.arenaSelectActor` já recusa abrir o popup de uma entrada de outro Treinador), confirmado nesta leva sem precisar de mudança de código. **Notificações de combate como popup** (`arenaErrorPopupHTML`, `.arena-error-overlay`/`.arena-error-overlay-card`) — qualquer coisa que já ia pro banner do topo (`SESSION.error`, ex. "sem PP") agora também aparece como popup central, chamando mais atenção, além do banner de sempre. **Botão Curar da ficha, só Mestre, cura tudo de uma vez** — `pokeCardHTML` trocou a condição do botão de `editable` pra `canGM`; `DT.healFull` agora também zera `status`/`statusTurns`/`confuso` e recarrega o PP Atual de cada Movimento pro Máximo, além de restaurar o PV (antes só fazia PV). **Turno/PV separados no card + barra de vida** — `.arena-turn-badge` saiu de `position:absolute` (flutuava por cima do PV) pra um bloco normal no fluxo, renderizado ANTES do resto do card; `.arena-slot`/`.arena-slot-hidden` ganharam mais `gap` e uma `.hp-bar` própria, mais alta, abaixo do número atual/máximo. **Ficha mais larga** — `.shell` foi de `max-width:960px` pra `1056px` (+48px, ~meia polegada por lado), pra segurar melhor o menu de ver os Pokémon sem espremer.
- O que fica de fora, por enquanto: Movimentos de campo (Stealth Rock, clima), Baton Pass, Jungle Healing/Purify, e o efeito mecânico de verdade dos itens catalogados como `'outro'` (Pokébolas, Itens de Batalha/Evolução/Segurados, Itens-Chave, TMs) — isso continua com o Mestre.

## Área PVP (combates rápidos, sem Mestre)

Botão **"⚔️ PVP"** no topo — um modo à parte da Arena do RPG, pedido do usuário pra "combates rápidos" que "não dependa de mestre" e cujas regras de Pokémon/combate "vão ser atualizadas lá simultaneamente toda vez que eu fizer uma alteração no RPG, de tudo". Ainda exige login com uma conta de Treinador cadastrada (mesmo PIN de sempre) — só a partir daí é 100% self-service.

- **O motor de combate NÃO é copiado — é o mesmo código da Arena, chamado de verdade.** `withArenaContext(arenaObj, fn)` aponta `STATE.arena` temporariamente pra dentro de uma sala de PVP, chama `fn()` síncrono (o site inteiro roda em JS de thread única, sem `await` no meio de uma ação) e devolve o ponteiro pro normal no `finally` — o suficiente pra reaproveitar sem mudar uma linha `arenaLog`, `arenaQueueAction`, `arenaResolveNextStep`, `arenaComputeResolveOrder`, `arenaCheckVictory`, `applyEndOfTurnStatusAndLog`, `arenaStatusActionPreCheck`/`arenaConfusionPreCheck`, `eligibleMoveTargets`, `computeInitiativeOrder`, `arenaBuildSelectingRound`, `DT.resolveArenaMove` e `DT.arenaSwapInResolved` — o Cap. 86 inteiro (seleção simultânea, ordem por Prioridade/Iniciativa, Paralisia/Sono/Congelamento/Confusão, Habilidades, PP, Estágios) roda idêntico dentro de uma sala. Qualquer ajuste futuro nessas funções (uma correção de regra, um novo gatilho de Habilidade) já vale pra PVP também, sem precisar lembrar de mexer em dois lugares — não existem duas cópias do motor.
- `findTrainer(id)` ganhou um fallback: além de `STATE.trainers` (Treinadores de verdade), reconhece um "treinador virtual" em `STATE.arena.virtuais[id]` quando existir — só populado enquanto `STATE.arena` estiver apontando pra uma sala via `withArenaContext`. Pro motor de combate, um treinador virtual de PVP é só mais um Treinador qualquer; fora de qualquer sala (`STATE.arena` de volta à Arena normal do RPG), esse fallback nunca é alcançado — zero risco pro código existente.
- **Bug real achado pelo usuário ("a Arena tá impactando o PVP")**: `withArenaContext` troca `STATE.arena` só durante a chamada SÍNCRONA de `fn()` — mas o quadro de dado da Arena (`#diceOverlay`) anima em `setInterval`, ASSÍNCRONO. Como nenhum timer dispara no meio de uma chamada síncrona, o 1º frame do quadro de dado (dentro do próprio `withArenaContext`) mostrava a sala certa, mas todo tick seguinte (90ms depois, já fora do `withArenaContext`, com `STATE.arena` de volta ao normal) relia `STATE.arena.lastRoll` — a Arena de VERDADE, não a sala — mostrando dado errado (ou nada) pro resto da animação, TODA VEZ que alguém agia na Área PVP. Corrigido com `ARENA_CONTEXT_DEPTH` (contador incrementado/decrementado por `withArenaContext`): `checkArenaRoll()` e `renderDiceOverlay()` agora saem cedo enquanto ele for >0 — a Área PVP nunca precisou desse quadro mesmo (a Rodada resolve tudo de uma vez, sem pausa nenhuma pra ver o dado rolando), então bloquear ali não tira nada dela, só corta o vazamento pra Arena de verdade. Mesma trava em `arenaResetActionState()` — sem ela, um caminho raro dentro de `DT.resolveArenaMove` (Movimento sem alvo válido na hora de resolver) zerava o popup de Ação da Arena de VERDADE se a mesma pessoa tivesse um aberto (mesma sessão/aba, outro momento) bem na hora de uma Rodada de PVP resolver. Testado (`test_pvp.js`): confirmado que o bug realmente acontecia sem a trava (regressão provada desativando-a manualmente) e que os dois caminhos ficam mudos/intactos com ela.
- **Modelo de dados**: `STATE.pvp = { fichas: {[trainerId]: [...] }, salas: {[salaId]: {...}} }`, dentro do mesmo `STATE` sincronizado de sempre (sem storage novo). `fichas[trainerId]` é o "box" de Pokémon de PVP daquele Treinador — **independente** do Time do RPG (`t.pokemons`/`t.banco`), criado/editado/removido pelo próprio jogador (`DT.pvpAddFicha`/`pvpRemoveFicha`/`pvpSetFichaField`/`pvpSetNatureza`/`pvpSetHabilidade`/`pvpSetMoveField`), sem Mestre nenhum por trás. Cada `sala` guarda **exatamente a mesma forma** que `STATE.arena` (`teams`, `entries`, `round`, `log`, `vencedor`, `fugaTravada`) mais `jogadores` (lobby) e `virtuais` (um treinador virtual por jogador, só dessa sala) — é o que permite apontar `STATE.arena` pra ela via `withArenaContext` sem tradução nenhuma.
- **Nível fixo, Atributos sempre no máximo** (resposta do usuário às perguntas de escopo): toda ficha de PVP nasce com `vinculo=25` travado (nunca editável — nem existe o campo na tela) e, ao entrar numa sala, ganha um treinador virtual com os 6 `attrPoints` em 5/5 (`pvpMaxTrainer()`/`sala.virtuais[vid]`) — como `hpMax`/`effectiveStat` já são funções puras de `(trainer, poke, stat)`, isso já entrega o Atributo Efetivo máximo sem mexer nas fórmulas. O jogador só escolhe **Espécie, Movimentos, Natureza e Habilidade** (mesmos `applySpeciesData`/`findNatureByName`/`habilidadesSelecionaveisDaEspecie` da ficha do RPG — mesma base oficial da PokeAPI, então atualização de dado de espécie também já vale pro PVP automaticamente).
- **"Nível 16" de verdade, não só um número solto** (correção do usuário — "os pokémon do PVP tem que estar todos no nível máximo do RPG, ou seja 16"): `var PVP_LEVEL = 16` é literalmente o próprio teto de Nível de Treinador do sistema (`attrBudget`/`officialLevelCeiling` já travam em `clampInt(nivel,1,16)` — 16 não é um valor inventado pro PVP, é o mesmo 16 que qualquer ficha do RPG esbarra ao chegar no topo). Usado só pra alimentar `officialLevelCeiling(PVP_LEVEL)`, o teto de Nível oficial que filtra o Learnset de Movimentos aprendidos por Nível.
- **Movimentos restritos ao Learnset real da Espécie — nunca mais um nome digitado à mão**: o builder de ficha (`pvpFichaCardHTML`) trocou o campo de texto livre (que deixava, por exemplo, um Charizard ganhar Hydro Pump) pelo **mesmo seletor "Learnset" do Treinador no RPG** (`movePickOptions(LEARNSETS[especieNum], metodo, officialLevelCeiling(PVP_LEVEL))`, botões Nível/Evo/TM/Egg) — o jogador só escolhe entre as opções que a própria PokeAPI (via `LEARNSETS`) diz que aquela Espécie aprende de verdade, no método escolhido. `DT.pvpSetMoveMethod(pid,idx,metodo)` troca o método (limpando o slot — o Movimento anterior pode não existir no novo método); `DT.pvpSetMoveSelect(pid,idx,el)` valida de novo no servidor (função) que o nome escolhido está de fato em `movePickOptions` daquele método/teto antes de aplicar — nunca aceita um Movimento fora da lista, mesmo chamado direto (defesa em profundidade, igual ao resto do motor).
- **Times completos, até 6, com troca** (resposta do usuário): cada sala tem exatamente 2 jogadores (Time A/B); no lobby de cada sala (`pvpTeamPickHTML`), cada um escolhe até 6 fichas do próprio box (`DT.pvpToggleFichaNoTime`) e clica **"✓ Pronto"** (`DT.pvpFicarPronto`) — assim que os DOIS estiverem prontos, `pvpTryStartBattle` monta os treinadores virtuais (cada um recebendo as fichas escolhidas como `pokemons`, igual a Equipe ativa do RPG) e a Rodada 1 já começa sozinha, sem ninguém clicar "Iniciar Batalha". Trocar durante a batalha reaproveita o mesmo caminho do RPG (`DT.pvpTrocarEscolher`→`arenaQueueAction{kind:'trocar'}`, `DT.pvpTrocarForcado`→`DT.arenaSwapInResolved` pra quem desmaiou ou tem um convite de troca livre pendente tipo U-turn).
- **Turno passa sozinho assim que os dois agem — sem Mestre nenhum controlando** (resposta do usuário): `arenaQueueAction` (reaproveitada sem mudança) já vira a Rodada de `'selecting'` pra `'resolving'` sozinha assim que todo mundo elegível escolheu — isso nunca dependeu do Mestre, nem no RPG. O que o RPG tornou manual foi só o AVANÇO passo a passo depois disso (Mestre clicando "▶️ Avançar", ver a seção da Arena acima); pra PVP, `pvpDriveResolution(sala)` chama `arenaResolveNextStep` num laço até a Rodada sair de `'resolving'` (terminou e já abriu a seleção da próxima sozinha, ou a batalha acabou) — a Rodada inteira resolve de uma vez, sem clique nenhum de ninguém no meio.
- **Várias salas em paralelo** (resposta do usuário): cada sala tem o próprio `entries`/`round`/`log`/`virtuais` isolado — duas batalhas simultâneas nunca se enxergam. O id do treinador virtual é `pvp_<trainerId>_<salaId>` (prefixo `pvp_` nunca bate com um id real de `STATE.trainers`, sufixo por sala evita colisão entre salas do mesmo Treinador). As fichas em si **não são clonadas** — `sala.virtuais[vid].pokemons` guarda referência direta pra `STATE.pvp.fichas[trainerId]` (dano/status/PP mexem na ficha de verdade), então uma ficha ganha `emBatalha=salaId` ao entrar numa sala e fica bloqueada pra qualquer outra sala em paralelo até essa batalha terminar.
- **Cura automática ao fim + Desistir** (pedidos do usuário): `pvpFinishBattle(sala)` roda sempre que `sala.vencedor` é decidido (vitória por PV zerado, igual ao RPG, OU por Desistir) — `pvpHealAll` zera PV/Condição/Confusão/PP/Estágios de toda ficha envolvida (`resetStages` reaproveitado) e libera `emBatalha=null`. Cada jogador tem o próprio botão **"🏳️ Desistir"** (`DT.pvpDesistir`) — declara o Time do outro jogador vencedor na hora, sem esperar o combate terminar sozinho; sair da sala no meio de uma batalha sem clicar Desistir (`DT.pvpSairDaSala`) conta como desistência do mesmo jeito, pra ninguém ficar esperando pra sempre. Sala esvaziada (todo mundo saiu) é removida de `STATE.pvp.salas` — sem lixo acumulando com o tempo.
- **Escopo desta primeira leva**: 2 jogadores por sala (não N-em-N), só Atacar/Trocar como ações (sem Item/Fugir — "Desistir" cobre "quero parar antes" no nível da sala inteira, e a Trava de Fuga da sala já nasce ligada), e a resolução roda inteira de uma vez (sem a pausa/animação passo a passo do Mestre da Arena do RPG — não fazia sentido sem ninguém controlando o ritmo).
- Testes: `test_pvp.js` (harness Node dedicado, mesmo `eval()` do `test_battle.js`/`test_render.js`) cobre fichas self-service, `withArenaContext`/`findTrainer` isolando o treinador virtual, o motor de dano/tipo reaproveitado rodando de verdade dentro de uma sala sem vazar log/estado pra Arena do RPG, resolução automática (sem clique), múltiplas salas em paralelo (com trava de ficha em uso), Desistir e cura automática, e limpeza de sala vazia. `test_render.js` ganhou um smoke test da tela nova (login exigido, Fichas + Lobby, criar ficha, expandir card, criar sala → tela de montar time, sair da sala apaga ela).

## O que já está no sistema de regras

Todo o núcleo de combate (atributos, Vínculo, PV, Movimentos, dano, tipos, iniciativa, Prioridade — motor de turno em seleção simultânea + resolução manual em lote (Cap. 86): todo mundo escolhe a própria ação ao mesmo tempo, sem "vez" sequencial nem negociação de Prioridade nenhuma, e o Mestre resolve a Rodada na ordem certa (não-Ataque sempre antes de Ataque; entre Ataques, Prioridade decide antes de Iniciativa) clicando "▶️ Avançar" um Pokémon por vez, sem timer nenhum — e os Movimentos de carregar 2 turnos (Cap. 25, Solar Beam e companhia) —, condições de status — Paralisia/Sono/Congelamento/Confusão testados sozinhos, dano de status por turno automático, Magic Guard bloqueando qualquer PV indireto de forma consistente —, Recuo/Dreno/Movimentos de cura na porcentagem oficial, Movimentos que trocam de campo depois de atacar, Desmaio pedindo o próximo Pokémon e vitória automática quando um Time inteiro cai), a progressão de Nível por batalhas vencidas, a Pokédex completa (Gen 1–9) e o banco de Movimentos oficiais (cada um com descrição em português), a organização de Equipe/Banco por arrastar, o fluxo guiado de entrada na Arena (Time → Soltura em segredo → Revelação simultânea, Cap. 17/55), e uma Mochila de itens por Treinador (gerenciada pelo Mestre) organizada em 5 abas com tabela (Poções e Berries, Pokébolas, Itens de Missão, TM, Inventário Geral), com o catálogo oficial completo por categoria (`ITEM_DEX`, 417 itens em 11 categorias — incluindo as 229 TMs vigentes de Scarlet/Violet, sempre disponível por inteiro), cada item com descrição em português (item de cura já citando o valor real do sistema, não o oficial do jogo) que autocompleta sozinha ao ser escolhido — "+ Adicionar" é só escolher da lista e clicar, sem campo nenhum pra preencher na mão. Um botão **"▶️ Usar"** em cada linha (pro dono da ficha ou o Mestre) pula direto pra escolher o Pokémon certo pra aquele item, sem precisar abrir a Cura e achar o item de novo lá. Cinco tipos de item têm efeito de verdade: cura de PV, de Condição/Confusão e reanimação de desmaiado (Vida/Condição/Reviver/Ambos, com a trava de que só Reviver funciona em quem está desmaiado) e recuperação/aumento de PP (Ether/Max Ether/Elixir/Max Elixir/PP Up/PP Max) — fora de batalha na aba Cura ou dentro da Arena; um sexto tipo, Outro (Pokébolas, pedras, TMs etc.), segue o mesmo fluxo de escolher o Pokémon mas ainda sem efeito mecânico de verdade (Cap. 25). Também Naturezas (ajuste automático de Atributo Efetivo), Habilidades reais por espécie restritas e editáveis só pelo Mestre — 93 delas com gatilho mecânico de verdade na Arena (contra-ataque no contato, imunidade/absorção de Tipo, bônus de dano, entrada/saída de campo, fim de turno, condição espelhada, custo de PP...), as demais só texto de referência —, PP real em todos os 847 Movimentos (1 por uso, 2 contra Pressure, bloqueia em 0, **só o Mestre edita direto**) e Estágios automáticos por Movimento (dado oficial de mudança de Estágio, com as mesmas reações de Habilidade do Cap. 6.3, resetando a 0 sempre que o Pokémon sai de campo — desmaia, troca ou foge —, **também só o Mestre edita direto**). Ainda em aberto: Maturidade/Evolução definitiva, Captura, Struggle automático, clima/terreno, item equipado, Baton Pass, Jungle Healing/Purify, e efeito mecânico dos itens catalogados como `'outro'` (Pokébolas, Itens de Batalha, Itens de Evolução, Itens Segurados, Itens-Chave, TMs).
